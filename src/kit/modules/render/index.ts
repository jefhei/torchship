/**
 * M2-T2 — render barrel for the authored modules.
 *
 * One component per authored module (the M2-T7 harness mounts these
 * standalone); geometry always comes from the module's own parts list.
 */

export * from './HeadModule'
