{
  "project": "torchship",
  "tasks": {
    "M1-T1": {
      "status": "complete",
      "date": "2026-09-04",
      "title": "Scaffold app (Vite + TS + R3F); app boots with an empty viewport",
      "files_modified": [
        "src/App.tsx",
        "src/App.css",
        "src/Viewport.tsx",
        "src/App.test.tsx",
        "README.md"
      ],
      "tests_added": [
        "src/App.test.tsx \u2014 4 tests: header renders title+class line; vessel name chip (Firebrand); viewport + R3F canvas mounted in <main>; ready chip appears when Canvas onCreated fires (boot path)"
      ],
      "verification": "lint 0w/0e; typecheck clean; test 5/5 passed (2 files); build green; prettier clean on touched files",
      "notes": "R3F <Canvas> viewport wired into the shell with an EMPTY scene (clear color only) \u2014 kit/assembler content lands in M2/M3. onCreated flips a 'viewport ready' chip so boot-vs-blank is distinguishable headlessly. Ship name locked per BUILD_PLAN rule 9: Hound-class light corvette 'Firebrand' (header chip + README; goes into ShipSpec name field at M1-T2, share URL at M6). Tests mock @react-three/fiber Canvas (jsdom has no WebGL)."
    },
    "M0-T2": {
      "status": "complete",
      "date": "2026-09-05",
      "title": "Spike 2 (seams): join all 6 module types in a stress spec with offsets; measure seam gaps + socket misalignment; fix [auto] tolerances from real numbers",
      "files_modified": [
        "src/spikes/seams/vec.ts",
        "src/spikes/seams/kit.ts",
        "src/spikes/seams/measure.ts",
        "src/spikes/seams/stress.ts",
        "src/spikes/seams/tolerances.ts",
        "src/spikes/seams/seamStress.test.ts",
        "docs/spikes.md"
      ],
      "tests_added": [
        "src/spikes/seams/seamStress.test.ts \u2014 13 tests: exactness (78 socket-solved joins @ zero offset all \u2264 float eps); kit scanner flags engineering.high-hatch@1.2m; spine-face flush mating per room type; normal/lateral/vertical offset response slope 1.000 under both policies; noise \u2264 2\u03b5 bounds (door vertical + far-face shift); \u00b10.5 mm margin claims (\u22652\u00d7 watertight, \u22655\u00d7 hatch); \u00b11.0 mm = zero-headroom ceiling; pathological reject set (10/25 mm offsets + 200 mm authored high-hatch step); tolerance constants exported"
      ],
      "verification": "lint 0w/0e (14 files); typecheck clean; test 18/18 passed (3 files); build green; prettier clean on new files",
      "notes": "VERDICT: proven. White-box stand-ins for all 6 module types joined socket-to-socket: 78 joins @ zero offset \u2192 max seam gap/lateral/vertical/floorStep = 0.000000 mm (join math is never the error source). Offset response linear & axis-separated (normal \u03b4 \u2192 open seam \u03b4; lateral \u03b4 \u2192 misalign \u03b4; vertical \u03b4 swallowed by floor pin / becomes door-miss+floor-step under door solve). Authoring noise \u00b10.5 mm \u2192 \u22641.0 mm bounds (2\u00d7 margin vs 2 mm watertight cap, 5\u00d7 vs 5 mm hatch cap; \u00b11.0 mm = zero headroom \u2192 0.5 mm is the authoring ceiling). Pathological joins (\u226510 mm offsets; engineering 1.2 m high-hatch vs standard 1.0 m door \u2192 200 mm step) exceed both caps \u2192 validator reject set. Decision: sockets are the ONLY join interface (M3-T2 'never freehand' now measured, not asserted); floor-pinned assembly (walkable deck plates) with XZ from sockets; tolerances fixed in src/spikes/seams/tolerances.ts (imported by M0-T6/M1-T3): watertightGapMm 2, hatchAlignMm 5, maxSocketAuthoringErrorMm 0.5, standardDoorCenterM 1.0. Full verdict + measured report card in docs/spikes.md."
    },
    "M1-T2": {
      "status": "complete",
      "date": "2026-09-06",
      "title": "Define data-contract types in src/types/: ShipSpec, ModuleRef, DoorSocket, module-kit manifest, scene graph, material slots",
      "files_modified": [
        "src/types/geometry.ts",
        "src/types/units.ts",
        "src/types/ship.ts",
        "src/types/kit.ts",
        "src/types/scene.ts",
        "src/types/materials.ts",
        "src/types/index.ts"
      ],
      "tests_added": [
        "src/types/geometry.test.ts \u2014 4 tests: FACING_VEC four facings/unit; rotY axis quarter-turns exact; yaw leaves Y untouched + XZ length preserved; quarter-turn composition (2\u00d7=half, 4\u00d7=identity)",
        "src/types/units.test.ts \u2014 3 tests: MM + deck clear/pitch conventions; standard door sits bottom edge on floor (1.0 \u2212 2.0/2 = 0); deck floors descend by pitch, monotone",
        "src/types/kit.test.ts \u2014 13 tests: deck-offset helper (standard 0, engineering high-hatch +0.2, \u22120.2); door-size default vs explicit; isStandardDoorCenter; getKitModule find/throw; manifest integrity: intact manifest passes, dup module ids, dup door/light/equipment ids, non-positive dims, socket off-face > \u00b10.5 mm (and inside-budget ok), door overhanging face, door not fitting floor-to-ceiling, light/equipment outside box, assert throws listing problems",
        "src/types/materials.test.ts \u2014 6 tests: vocabulary = 9 PRD \u00a77 slots unique; MaterialSlots<T> complete-only (compile-time @ts-expect-error on omission); assert passes 9-slot theme; throws listing every unassigned slot; throws on present-but-undefined; ignores unknown extra keys",
        "src/types/barrel.test.ts \u2014 2 tests: barrel re-exports runtime helpers (MM, rotY, MATERIAL_SLOTS, getKitModule) and type surface (ShipSpec/ModuleRef/MaterialSlot compile from '../types')"
      ],
      "verification": "lint 0w/0e (26 files); typecheck clean; test 46/46 passed (8 files, +28 new); build green; prettier clean on new files only",
      "notes": "All PRD \u00a77 contracts typed and checked in under src/types/ (barrel at src/types/index.ts): geometry primitives (Vec3/Rotation/Facing + FACING_VEC/rotY, Y-thrust conventions from the proven M0-T2 vec), units (MM, deck 3.0 clear/3.2 pitch, standard door 0.9\u00d72.0 @ 1.0 m center, deckFloorYFor), ShipSpec/DeckSpec/ModuleRef (decks ordered nose\u2192aft, yPosition explicit, ref offset.y reserved 0 = floor-pinned), kit manifest (KitModule: dimensions/doorSockets/lightSockets/equipmentSlots/collisionHint + DoorSocket position/facing/deck-offset; doorCenterDeckOffsetM is the contract's deck offset; authoring frame: floor y=0, XZ footprint centered), scene graph (DeckNode: merged geometry groups by material slot, instance batches, named interactive transforms, per-deck collision hull), material slots (9-name vocabulary + MaterialSlots<T> mapped type where omission = compile error + runtime assertMaterialSlotsComplete for JSON themes \u2014 M1-T4 CI hook and M2-T7 harness build on it). Kit-manifest integrity checker (kitManifestProblems/assertKitManifestIntegrity) validates authoring frame incl. socket flush within the M0-T2 measured \u00b10.5 mm budget (imports SEAM_TOLERANCES \u2014 no re-derivation). 28 new tests pin every helper. No three.js in the contracts (manifest-level; renderer attaches buffers by id at M3). NEXT: M0-T5 (canonical fixtures \u2014 Patrol/Long-Haul/Science + pathological stress spec), which unlocks M0-T6 + M1-T3."
    },
    "M0-T5": {
      "status": "complete",
      "date": "2026-09-07",
      "title": "Define canonical test ships & check them in: (a) Patrol 5-deck, (b) Long-Haul stretch, (c) Science retrofit, (d) pathological offset-hatch stress spec",
      "files_modified": [
        "src/fixtures/layout.ts",
        "src/fixtures/patrol.ts",
        "src/fixtures/longHaul.ts",
        "src/fixtures/science.ts",
        "src/fixtures/stress.ts",
        "src/fixtures/registry.ts",
        "src/fixtures/index.ts",
        "src/fixtures/fixtures.test.ts"
      ],
      "tests_added": [
        "src/fixtures/fixtures.test.ts \u2014 24 tests: registry order/lookup/expectValid split; identity fields; unique deck ids + populated decks; room-module-id vocabulary (spine never referenced); monotone nose\u2192aft floor descent; JSON round-trip (preset-JSON ready); patrol 5-deck hero sequence + crew/galley spawn deck at index 1; long-haul 6-deck storage x2; science ops-module retrofit; canonical grid y = deckFloorYFor(i); every ref floor-pinned flush at atSpine() pose; attach-geometry spot checks (0.7 + depth/2); stress rig control deck clean, 5 defect categories on rig-1..rig-4 at declared magnitudes (10 mm / 25 mm / 200 mm / 0.2 m / 50 mm), floor-pin violation isolated"
      ],
      "verification": "lint 0w/0e (34 files); typecheck clean; test 70/70 passed (9 files, +24); build green (321ms); prettier clean on src/fixtures/",
      "notes": "Fixture layout convention (for M1-T3/M3): spine shaft center at deck-local origin on EVERY deck (continuous vertical run); rooms attach rot-0 to the +z face, offset z = SPINE_HALF_M 0.7 + depth/2, offset.y = 0 (floor-pinned); door centers land at (0, 1.0, 0.7) = spine band +z socket. ROOM_FOOTPRINTS table = contract-level bounds M2 must reproduce (M2-T7 gate), stand-in until the real KitManifest. stress.ts rig-3 pair arithmetic: engineering high-hatch local [2.25, 1.2, 0.6] (+x) vs ops side-door local [-1.95, 1.0, -1.0] (-x) coincide in XZ at 4.2 m x / 1.6 m z center offset \u2014 200 mm vertical step. expectValid:false = must-reject for M1-T3 validator + M0-T6 stubs; expectValid fixtures must pass. Socket-residual math itself is M1-T3, not this task."
    },
    "M0-T6": {
      "status": "complete",
      "date": "2026-09-08",
      "title": "Stub [auto] invariant checks (PRD \u00a78) as runnable tests against the fixtures",
      "files_modified": [
        "src/invariants/registry.ts",
        "src/invariants/specAnalysis.ts",
        "src/invariants/checks.ts",
        "src/invariants/harness.ts",
        "src/invariants/index.ts",
        "src/invariants/invariants.test.ts"
      ],
      "tests_added": [
        "src/invariants/invariants.test.ts \u2014 31 tests: registry contract (6 invariants in \u00a78 order, bullets 1-6 unique, tolerances pinned to SEAM_TOLERANCES w/ drift guard 2/5 mm, live-vs-stub split, owner milestones M1-T3/M2-T7/M3-T2/M3-T3/M3-T6, LIVE_CHECKS keys == live ids, getAutoInvariant throws); spec analysis (atSpine refs 0.000 mm all channels across real ships, stress magnitudes exactly 10/25/200 mm, rotated/non-room refs unattributable, hatch cap inclusive \u22645 mm, seat problems wording, per-deck seat summaries incl. failing rig decks 1/2/4, deck-grid errors 0 vs 50 mm, deckHosts/findDeckHosting); live checks on fixtures (spine-connectivity passes 3 real ships w/ deck counts + endpoints, fails stress naming rig-1/rig-2/rig-4 + 25.0 mm + 50.0 mm grid; spawn-inside passes real ships, fails stress no-galley crew deck); synthetic mini-specs prove real logic (canonical head+galley+engineering passes; 25 mm-lateral galley fails both live checks; headless deck 0; single-deck; no-engineering; 50 mm off-grid floor); harness (6 attributable runs per fixture in \u00a78 order, real ships pass spine+spawn w/ 4 deferred, stress fails spine+spawn, deferred details name owner milestones, full harness = 24 runs, per-invariant columns 3 pass/1 fail)"
      ],
      "verification": "lint 0w/0e (40 files); typecheck clean; test 101/101 passed (10 files, +31); build green (341ms); prettier clean on src/invariants/ (src/index.css pre-existing format warning untouched)",
      "notes": "PRD \u00a78 [auto] invariants declared as a typed, ordered target set in src/invariants/registry.ts (AUTO_INVARIANTS, ids seams-watertight/hatch-alignment/spine-connectivity/collision-match/spawn-inside/room-lit; caps imported from M0-T2 SEAM_TOLERANCES \u2014 never re-derived). 4 stubs = declared targets w/ owner + required input, reported 'deferred' by the harness (seams-watertight\u2192M3-T2 assembled geometry; hatch-alignment\u2192M1-T3 kit sockets+resolver incl. 1.2 m high-hatch; collision-match\u2192M3-T3 hulls (10 cm); room-lit\u2192M2-T7 lightSockets). 2 LIVE spec-level checks (checks.ts): spine-connectivity = run continuity (every deck seats a module on the shaft band w/in 5 mm hatch cap on lateral/vertical/face channels + floors on the canonical grid so bands stack at 3.2 m pitch + head@deck0/crew@deck1/engineering endpoints \u2192 crew\u2192head & crew\u2192engineering reachable); spawn-inside spec facet = crew deck (index 1) hosts a galley seated at the spine foot (containment half \u2192 M3-T6). Coarse seat rule (specAnalysis.ts) measures the M0-T2 channels on the canonical rot-0 spine-door pose only; rotated/side-mate/non-standard-height sockets are M1-T3 resolver territory (documented per check). Harness (harness.ts) never throws: runInvariantHarness(fixture)=6 runs in \u00a78 order; runInvariantHarnessAll()=24. Verdicts on fixtures: 3 real ships pass spine+spawn (4 targets deferred); the stress rig FAILS spine-connectivity at rig-1 (10 mm face gap), rig-2 (25 mm lateral), rig-4 (200 mm floor-pin drift + 50 mm off-grid floor) and spawn-inside (no galley at index 1) \u2014 4 of its 5 declared defect categories are caught by live spec checks; the rig-3 200 mm module-to-module high-hatch step stays with the M1-T3 stub (needs kit socket data). M1-T3 wires hatch-alignment by adding its check to LIVE_CHECKS; stub entries are the slot contract. NEXT: M1-T3 (spec validator + preset JSON)."
    },
    "M1-T3": {
      "status": "complete",
      "date": "2026-09-09",
      "title": "Ship Spec validator (schema check, socket alignment, spine connectivity graph) + preset JSON for the four fixtures",
      "files_modified": [
        "src/validation/contractKit.ts",
        "src/validation/sockets.ts",
        "src/validation/validator.ts",
        "src/validation/index.ts",
        "src/validation/validation.test.ts",
        "src/presets/presets.test.ts",
        "public/presets/patrol.json",
        "public/presets/long-haul.json",
        "public/presets/science.json",
        "public/presets/stress.json",
        "src/invariants/registry.ts",
        "src/invariants/checks.ts",
        "src/invariants/invariants.test.ts"
      ],
      "tests_added": [
        "src/validation/validation.test.ts \u2014 31 tests: CONTRACT_KIT integrity/dims/M0-T2 socket origins (engineering high-hatch = only non-standard door); socket resolution (band doors on the grid, canonical spine door lands 0.000 mm, side-door world poses, rotated 180\u00b0 module lands on the \u2212z band face, stress magnitudes exactly 10/25/200 mm, opening-overlap gating); schema rules (identity, dup/empty decks, non-descending + off-grid floors, unknown modules, bad rotation, floor-pin); socket rules (real ships + mini pass with side doors legally blanked; 10 mm proud / 25 mm lateral / 200 mm floor-pin drift caught; rig-3 high-hatch\u2194side-door pair = 200 mm vertical step; door onto a blank wall = dangling only when the opposing hatch is absent); spine graph (real ships pass, stress unseated decks rig-1/2/4 named, \u00a78 endpoints, verdicts agree with the M0-T6 live check on all four fixtures); fixtures accept/reject (expectValid \u2192 empty problems; stress rejected naming every STRESS_DEFECTS kind; rig-0 control deck never a problem)",
        "src/presets/presets.test.ts \u2014 3 tests: sync (semantic compare, golden-file rewrite on fixture drift), checked-in files parse back toStrictEqual the fixture objects, file names patrol\u2192stress",
        "src/invariants/invariants.test.ts \u2014 3 new + updated rows: hatch-alignment LIVE (pass detail pins all-5-spine-doors + 4-blanked sides on patrol; stress fails at rig-1/2/3/4 incl. the 200 mm pair step, rig-0 clean), harness rows now [deferred,pass,pass,deferred,pass,deferred] real ships / [deferred,fail,fail,deferred,fail,deferred] stress, stub owners drop hatch-alignment, synthetic pushed-spine-door fails the live check"
      ],
      "verification": "lint 0w/0e (46 files); typecheck clean; test 138/138 passed (12 files, +37); build green (352ms); prettier clean on touched files incl. public/presets",
      "notes": "Ship Spec validator in src/validation/ (barrel index.ts): generic validate(spec, kit) \u2014 shipSpecProblems/isValidShipSpec/assertValidShipSpec \u2014 three rule groups. (1) schema: identity fields, \u22651 deck, unique non-empty deck ids, populated decks, strict nose\u2192aft floor descent, canonical deckFloorYFor(i) grid (off-grid deck = a step in the spine run), refs name known kit modules, offset.y reserved 0 (floor-pin), quarter-turn rotations. (2) socket alignment = PRD \u00a78 bullet 2 hatch-alignment, now LIVE in the invariant harness (registry status live, LIVE_CHECKS checkHatchAlignment, 3rd live check): world-space resolver (sockets.ts) measures the M0-T2 normal/lateral/vertical channels (mm, 0.001 rounding) between door centers; rules: every room spine-door must LAND on its deck spine-band socket \u22645 mm on every channel (rig-1 10 mm proud face gap, rig-2 25 mm lateral, rig-4 200 mm vertical drift caught); module-to-module pairs whose faces engage (opposing, \u226450 mm plane separation) AND openings overlap must align \u2014 rig-3's engineering high-hatch (1.2 m center) \u2194 ops side-door (1.0 m) caught as the 200 mm door-center step it is; a door opening onto another module's blank wall (no opposing socket) is DANGLING. Unjoined side sockets that engage nothing are LEGAL (blanked; real ships' 4-5 side doors per ship pass). (3) spine connectivity graph: every deck seats \u22651 module socket-resolved + \u00a78 endpoints head@deck0 / crew@deck1 / engineering \u2014 verdicts identical to the M0-T6 coarse live check on all 4 fixtures (cross-pinned). Socket source = CONTRACT_KIT (contractKit.ts): a genuine KitManifest reproducing ROOM_FOOTPRINTS dims + the M0-T2 white-box socket origins (spine doors flush at \u2212depth/2 standard 1.0 m; galley/ops/storage side doors; engineering high-hatch the ONLY non-standard door) \u2014 this is the expected-origins table M2-T7 gates the authored kit against. Fixture verdicts: patrol/long-haul/science problem-free (expectValid \u21d2 accept); the stress rig is REJECTED with every declared STRESS_DEFECTS category named at its deck (10 mm proud open seam / 25 mm lateral / 200 mm hatch step / floor-pin 0.2 m / 50 mm grid) + extras (ops spine door unlanded on rig-3) while rig-0 control stays clean. Preset JSON: public/presets/<fixture-id>.json for all four fixtures (registry id/label/description/expectValid + spec), generated/guarded by src/presets/presets.test.ts (semantic sync \u2014 prettier layout on nested arrays differs from JSON.stringify, so drift compare is parse-based; run prettier after a fixture edit). NEXT: M1-T4 (CI wiring + material-slot completeness check \u2014 assertMaterialSlotsComplete hooks already exist in src/types/materials.ts)."
    },
    "M1-T4": {
      "status": "complete",
      "date": "2026-09-10",
      "title": "Lint/format/build CI + vitest runner; material-slot completeness check (unassigned slot = failed build)",
      "files_modified": [
        "src/materials/theme.ts",
        "src/materials/themes.ts",
        "src/materials/index.ts",
        "src/materials/materials.test.ts",
        "scripts/check-material-slots.ts",
        "ci/github-actions-ci.yml",
        "package.json",
        "tsconfig.app.json",
        "src/index.css",
        "README.md"
      ],
      "tests_added": [
        "src/materials/materials.test.ts - 13 tests: slot vocabulary is the nine PRD \u00a77 slots (unique, ordered); registry has unique ids + getMaterialTheme lookup (unknown id throws); the build gate passes every registered theme (themesProblems == [] and assertThemesComplete does not throw); the default theme fills exactly the nine slots with distinct non-blank PBR set ids; omitting ANY single slot is caught naming that slot (loop over the vocabulary); assigned-but-blank payloads caught (blank/empty set, {}, null, string payload); undefined value = unassigned; theme id + label required (spaces-only id, empty label, non-object theme); duplicate theme ids across the registry; assertThemesComplete throws listing every problem with theme id + slot named; JSON themes (preset/export path) validate identically and round-trip toStrictEqual; themeIdOf only extracts a usable id; compile-time completeness of MaterialTheme.slots (@ts-expect-error on an omitted slot)"
      ],
      "verification": "npm run verify green end-to-end: lint 0w/0e (51 files, 104 rules); format:check clean repo-wide (fixed the pre-existing src/index.css wrap - it was the only warning before); typecheck clean; test 151/151 passed (13 files, +13 new); build green ~320ms including the new check:slots gate; negative path proven - a blank slot payload and a missing slot each make check:slots AND npm run build exit 1 with the theme id + slot named (temp break, reverted). CI RUNTIME NOT YET ACTIVE: the workflow file cannot be pushed with the credentials on this box (see notes) so it is checked in at ci/github-actions-ci.yml; git push of the code+trackers succeeded.",
      "notes": "M1-T4 lands the machine gate. (1) CI workflow authored (3 jobs on push(main)+PR: static = npm ci / lint / format:check / typecheck / check:slots; test = vitest run; build = npm run build; Node 22, npm cache). BLOCKER: the workflow file CANNOT live at .github/workflows/ci.yml yet - GitHub refuses any push that creates/updates .github/workflows/** unless the credential has the `workflow` scope (classic PAT) or `Workflows: Read and write` (fine-grained); the push was rejected with \"refusing to allow a Personal Access Token to create or update workflow .github/workflows/ci.yml without workflow scope\". Both credentials on this box are classic PATs with `repo` scope only (~/.git-credentials line and $GH_TOKEN; no SSH key is authorized, ~/.hermes/tokens/ is empty; see ~/notes/github-token-strategy.md, whose target Tier-1 machine token carries Workflows R/W). So the workflow is checked in at ci/github-actions-ci.yml with a header + README note giving the one-command activation: `git mv ci/github-actions-ci.yml .github/workflows/ci.yml` from a workflow-scoped credential, then push. CI does NOT run until then; npm run verify is the gate in the meantime. (2) Material-slot completeness = \"unassigned slot = failed build\" (PRD \u00a77) is now enforced twice: compile-time via MaterialSlots<T> (M1-T2) and runtime via assertThemesComplete() run by scripts/check-material-slots.ts, the first step of npm run build and its own CI step. (3) src/materials/ = the theme module: theme.ts (MaterialSlotSpec interim payload = one PBR set id per slot; MaterialTheme {id,label,slots}; themeProblems / themesProblems / assertThemesComplete / themeIdOf - the missing-slot rule DELEGATES to the M1-T2 assertMaterialSlotsComplete, never forked; adds blank-payload, missing id/label and duplicate-id rules), themes.ts (MATERIAL_THEMES registry + DEFAULT_THEME_ID \"firebrand\" + getMaterialTheme; the canonical Firebrand standard theme assigns all 9 \u00a74 slots to named set ids M4-T1 will author), index.ts barrel. (4) Distinctive technique: the build gate is a plain `node scripts/check-material-slots.ts` - Node >= 22.18 strips types natively and tsconfig already had allowImportingTsExtensions + erasableSyntaxOnly, so the gate needs no tsx/esbuild toolchain; therefore relative imports under src/materials/ and scripts/ carry explicit .ts extensions (documented in the module headers - node ESM requires them). scripts/ was added to tsconfig.app.json include so it is typechecked/linted at full strictness. (5) package.json: new check:slots script, build = check:slots && tsc -b && vite build, verify now includes format:check. NEXT: M2-T1 (kit primitives - bulkhead, deck plate w/ cable runs, conduit, panel light, hatch, ladder segments, equipment props); M1 is now fully complete (no held spikes) so M2 opens. HUMAN ACTION ITEMS: (a) add the `workflow` scope (or provision the Tier-1 fine-grained machine token with Workflows R/W) and activate the CI file; (b) held M0 visual spikes M0-T1/M0-T3/M0-T4 remain [ ] for an interactive session."
    },
    "M2-T1": {
      "status": "complete",
      "date": "2026-09-11",
      "title": "Kit primitives: bulkhead, deck plate w/ cable runs, conduit, panel light, hatch, ladder segments, equipment props (lockers, screens, couches, table, coffee station)",
      "files_modified": [
        "src/kit/types.ts",
        "src/kit/parts.ts",
        "src/kit/catalog.ts",
        "src/kit/index.ts",
        "src/kit/render/transforms.ts",
        "src/kit/render/slotSurfaces.ts",
        "src/kit/render/SlotMaterial.tsx",
        "src/kit/render/KitParts.tsx",
        "src/kit/render/primitives.tsx",
        "src/kit/render/index.ts",
        "src/kit/parts.test.ts",
        "src/kit/catalog.test.ts",
        "src/kit/render/transforms.test.ts",
        "src/kit/render/slotSurfaces.test.ts"
      ],
      "tests_added": [
        "src/kit/parts.test.ts - 37 tests: bulkhead solid panel + standard doorway (piers/lintel bound the opening, solid area == W\u00b7H - door area) + raised doorway emits the sill + reject-no-panel/does-not-fit; deck plate walking surface at y=0 + centred cable runs + non-integer count rejected; conduit run bare cylinder + per-axis clamp size/positions + negative brackets rejected; panel light housing+emissive lens below centre + bad lensInset rejected; hatch socket-centred leaf + four-member frame + knee hazard stripe inside the leaf + clearance/frame rejected; ladder 2 rails + rungs derived from spacing (0.3..2.7, last rung stays inside the storey) + bounds + reject overflow/overlap; locker body+door+handle and a 3-door bank + zero doors rejected; screen bezel+panel + oversized bezel rejected; couch pedestal/seat/back/straps + negative straps rejected; table top + 4 pipe legs at 0.74/0.34 insets; coffee station carries coffee-accent+screen+panel-light and the carafe sits on the counter; heat shield stripe inset + oversized stripe rejected; partBounds rotation-aware (quarter-turn swaps X/Z) and per cylinder axis; partsBounds folds + refuses empty; partMaterialSlots dedups into canonical order; partsSize; -0 never escapes a centred build",
        "src/kit/catalog.test.ts - 15 tests: canonical BUILD_PLAN vocabulary order; unique ids/labels/non-degenerate boxes; integrity gate clean; declared bounds == default build bounds to 1e-9 (contained AND tight); declared materialSlots == partMaterialSlots(default build); the kit fills all nine \u00a74 slots; getKitPrimitive lookup + unknown-id throw; only the hatch fills a door socket; hatch default seals the standard 0.9\u00d72.0 opening; category groupings; primitiveBounds/primitiveDimensions; exhaustive default dispatch (unknown id throws); problems caught for dup id / blank label / empty box / no slots / duplicated slot / wrong slot order; assertKitPrimitivesIntegrity lists every problem; every default part carries a legal slot",
        "src/kit/render/transforms.test.ts - 3 tests: vec3Tuple copies (does not alias) the readonly tuple; boxEuler maps quarter-turn yaw onto +Y (0/1/2/3); cylinderEuler turns the default +Y cylinder onto x (roll Z) / y (identity) / z (roll X)",
        "src/kit/render/slotSurfaces.test.ts - 4 tests: the interim table fills all nine \u00a74 slots; every slot has a distinct well-formed hex colour + in-range metalness/roughness; panel-light and screen are the emissive slots and nothing else; slotSurface resolves by slot"
      ],
      "verification": "npm run verify green: oxlint 'Found 0 warnings and 0 errors'; prettier --check clean repo-wide (5 new files formatted via prettier --write); tsc -b --noEmit clean; vitest 210 passed / 17 files (was 151/13 \u2014 59 new); npm run build green (check:slots 9/9 assigned, tsc, vite built in 349ms; only the pre-existing >500 kB chunk warning)",
      "notes": "Kit primitives authored as pure part builders + an authored catalog + a thin R3F layer. Three-agnostic parts (BoxPart/CylinderPart in meters, tagged by \u00a74 MaterialSlot) keep geometry headless-testable (jsdom has no WebGL). cross-task seams M2-T2..T6 + M2-T7 must honour: hatch is SOCKET-CENTRED (local origin = door centre, so seat it ON the DoorSocket position); every other primitive's local frame is documented per builder (floor-standing ones grow from y=0); geometry is ALWAYS generated by the builders \u2014 never hand-place meshes (rule 8); slotSurfaces.ts tints are explicitly INTERIM until M4-T1 authors the PBR sets; the catalog gate (kitPrimitiveProblems/assertKitPrimitivesIntegrity + the bounds test) means changing a builder requires updating KIT_PRIMITIVES.bounds. The kit is not yet mounted in the app (Viewport scene stays empty until M3) and vite does not bundle it yet \u2014 M2-T2..T6 module components are the first importers; M2-T7 is the standalone harness."
    },
    "M2-T2": {
      "status": "complete",
      "date": "2026-09-12",
      "title": "Author head module (bridge: crash couches, sensor wall, consoles)",
      "files_modified": [
        "src/kit/modules/placement.ts",
        "src/kit/modules/types.ts",
        "src/kit/modules/integrity.ts",
        "src/kit/modules/head.ts",
        "src/kit/modules/registry.ts",
        "src/kit/modules/index.ts",
        "src/kit/modules/render/HeadModule.tsx",
        "src/kit/modules/render/index.ts",
        "src/kit/modules/placement.test.ts",
        "src/kit/modules/head.test.ts",
        "src/kit/modules/registry.test.ts",
        "src/kit/index.ts"
      ],
      "tests_added": [
        "src/kit/modules/placement.test.ts - 7 tests: placePart/placeParts (identity leaves the part field-for-field intact and never mutates the input; translation; quarter-turn yaw (x,z)->(z,-x) with the box bounds swapping X/Z extents; box yaw composition mod 4; cylinder re-axing on odd turns (X<->Z, Y is the yaw axis); signed-zero normalization)",
        "src/kit/modules/head.test.ts - 12 tests: the bridge is intact (moduleProblems == [] + gate throws nothing); manifest declares the contract footprint 4.8 x 3.0 x 3.6 with the label naming the bridge; the authored spine-door reproduces contractSocketOrigins()['head.spine-door'] exactly (M2-T7 diff empty) and is the single standardized 0.9x2.0 door at centre 1.0 flush in the -z face; the shell (deck plate + 4 bulkheads) builds TIGHT to the declared box {min [-2.4,-0.2,-1.8], max [2.4,3,1.8]}; every fixture assembly stays inside the module box; two crash-couch stations at x = \u00b11.2 facing +z (rot 0) at the sensor wall; a console desk between each seat and the wall, flush on the wall's inner face; the sensor array + both station displays rot-2 (emissive face into the room) flush at z = 1.7; the 7 equipment anchors equal their assemblies' placements (no drift); 8 practical light sockets (3 panel + 3 screen + 2 task) with the panel sockets on the real panel-light assemblies; the module draws exactly deckplate/bulkhead/conduit/panel-light/screen/hazard/webbing; the spine doorway prism is free of geometry; the collision hint equals one box per solid part (37) with the hatch excluded",
        "src/kit/modules/registry.test.ts - 11 tests: registry exposes AUTHORED_MODULES/AUTHORED_KIT/getAuthoredModule (throws on unknown ids); authoredKitProblems() and authoredKitProblems(..., CONTRACT_KIT) are clean + kitManifestProblems(AUTHORED_KIT) == []; defect injection catches duplicated module ids, duplicated/unnamed/empty assemblies, geometry escaping the box, a missing spine-door, a door off its -z face, a door off the contract origin, a wrong facing, a contract-unknown module (empty diff), a blocked doorway, a collision hint that has drifted (wrong count AND shifted boxes), an unlit room and a module with no landmark anchors"
      ],
      "verification": "npm run verify green: oxlint 0 warnings/0 errors (104 rules); prettier --check clean repo-wide; tsc -b --noEmit clean; 240 tests / 20 files pass (was 210/17 \u2014 +30 this task); check:slots 9/9 assigned; vite build built in 340ms (kit/ still unbundled until M2-T7/M3 import it, expected)",
      "notes": "Head module authored in src/kit/modules/head.ts as 20 named assemblies -> 70 kit parts (all from the M2-T1 builders, rule 8), 8 light sockets, 7 equipment slots, 37 solid parts = 37 collision boxes. New module-authoring layer: placement.ts (primitive-local -> module-local transform, the pure-data twin of the R3F <group>), types.ts (ModuleAssembly/AuthoredModule + moduleParts/moduleCollisionBoxes/moduleMaterialSlots/moduleBounds/findAssembly), integrity.ts (moduleProblems: manifest contract + assemblies + box containment with documented allowances (deck plate -0.2 m below the floor, hatch frame \u00b10.05 m across its socket plane) + exactly-one-spine-door + anchors + doorway clearance prism + collision-hint-is-derived; moduleContractProblems against src/validation/contractKit.ts), registry.ts (AUTHORED_MODULES/AUTHORED_KIT/getAuthoredModule/authoredKitProblems/assertAuthoredKitIntegrity, taking an optional contractKit so kit/ never imports validation/), render/HeadModule.tsx (renders moduleParts(HEAD_MODULE) inside a placement group). CROSS-TASK SEAMS: (1) the ROOM owns the hatch on its own spine-door socket \u2014 M2-T6's spine module must supply the shaft + per-deck sockets and NOT a second hatch; (2) collision boxes are one-per-solid-PART, not per assembly, so wall doorways stay open (a coarse per-assembly box would seal them) \u2014 M3-T3 consumes moduleCollisionBoxes(); (3) M2-T3..T6 copy this file shape (assemblies + anchors derived from the same placements) and M2-T7 gates the whole registry via authoredKitProblems(AUTHORED_MODULES, CONTRACT_KIT)."
    },
    "M2-T3": {
      "status": "complete",
      "date": "2026-09-13",
      "title": "Author galley/bunk module (table + coffee station landmark, bunks, head)",
      "files_modified": [
        "src/kit/parts.ts",
        "src/kit/parts.test.ts",
        "src/kit/modules/placement.ts",
        "src/kit/modules/placement.test.ts",
        "src/kit/modules/galley.ts",
        "src/kit/modules/galley.test.ts",
        "src/kit/modules/registry.ts",
        "src/kit/modules/registry.test.ts",
        "src/kit/modules/index.ts",
        "src/kit/modules/render/GalleyModule.tsx",
        "src/kit/modules/render/index.ts"
      ],
      "tests_added": [
        "src/kit/parts.test.ts \u2014 3 tests: off-centre doorway cut at door.centerX (opening edges exactly centerX +/- door.width/2, piers take up the slack either side 3.25/0.85 m, panel still spans the whole wall, solid area == W*H - door area, lintel underside at the door head); the centred build is bit-identical whether centerX is omitted or 0; offsets that push the doorway off the panel edge are rejected (2.2 m on a 5 m wall, a door exactly as wide as the remaining panel, non-finite centerX)",
        "src/kit/modules/placement.test.ts \u2014 1 test: placePoint rotates by rotY then translates, agrees with placePart for the same point, maps a cabinet's front-face point into module space (the coffee-station strip at yaw 2), and normalizes signed zeros",
        "src/kit/modules/galley.test.ts \u2014 19 tests: module integrity clean + gate throws nothing; contract footprint 4.2 x 3.0 x 5.0 with a galley/coffee-station label; CONTRACT_KIT diff empty and both door-socket origins equal contractSocketOrigins() (spine-door + side-door, position and facing); exactly one standardized spine door flush in the -z face; the contract side door in the +x face 1.2 m bow-ward; shell builds tight to the declared box; the starboard wall is CUT at the side-door origin (piers at z [-2.5, 0.75] and [1.65, 2.5], opening centre 1.2 / width 0.9, lintel from the 2.0 m door head); two berths stacked on the port wall running fore-and-aft (1.9 m long, mattress/backrest tops 0.90 and 1.85); the mess is a table with a bench either side facing it, all clear of both doorway prisms; the pantry locker bank is flush on the port wall with its doors proud on the room side; the coffee-station landmark sits on the forward wall facing the entry with the carafe on the counter and the warm accent reserved to it alone; the head is walled off by two partitions with a 0.7 x 1.9 m doorway whose prism no other assembly occupies; the head fittings (counter, cabinet on the return partition, mirror over the sink) all stay inside the stall; 13 equipment anchors equal their assemblies' placements; practical lighting only \u2014 4 ceiling panel sockets each over a real housing, the task socket coincident with the coffee station's own strip part, one screen socket on the mess display; material slots are exactly the eight the galley draws (ceramic unused); both spine and side doorway prisms are clear of solid geometry; the collision hint is the derived one-box-per-solid-part hull with both hatches excluded; every fixture stays inside the module box"
      ],
      "verification": "npm run verify green end-to-end: oxlint 0 warnings/0 errors (79 files, 104 rules); prettier --check clean repo-wide; tsc -b --noEmit clean; vitest 263 passed / 21 files (was 240/20 \u2014 23 new); npm run build green (check:slots 9/9 assigned, vite built in 350ms, only the pre-existing >500 kB chunk warning)",
      "notes": "DONE 2026-09-13: M2-T3 \u2014 authored the GALLEY/BUNK module (the crew's heart) in src/kit/modules/galley.ts: 26 assemblies -> 99 kit parts (all from the M2-T1 builders, rule 8) on the contract footprint 4.2 x 3.0 x 5.0. Layout read from the spine door: centreline walkway aft->bow; two berths stacked on the port wall aft (the couch primitive read as a bunk, mattress tops 0.45 / 1.40 m); the mess (bolted table + two benches facing it) on the starboard side; the pantry locker bank (2 doors) on the port wall amidships; the COFFEE STATION \u2014 the \u00a74 warm-accent landmark \u2014 centred on the forward wall facing the entry (the module's ONLY coffee-accent geometry, gated in galley.test.ts), carafe on the counter, its own task-light strip proud of the cabinet front; the HEAD is a partition-built stall in the forward-port corner (0.7 x 1.9 m doorway, sink counter + plumbing cabinet + mirror + ceiling panel light). 6 light sockets (4 panel / 1 task / 1 screen) and 13 equipment anchors derived from the placements; collision hint = one box per solid part (54). Door sockets reproduce CONTRACT_KIT exactly (spine-door [0, 1.0, -2.5] -z; side-door [2.1, 1.0, 1.2] +x) -> moduleContractProblems(GALLEY_MODULE, CONTRACT_KIT) == []. KIT EXTENSION (M2-T4/T5/T6 need it too): bulkheadParts now takes door.centerX (off-centre doorway \u2014 piers/sill/lintel shift by centerX/2, the panel's wall area is preserved, offsets that run off the panel edge are rejected) so a side door is CUT by the builder, never freehand; placement.ts gained placePoint (primitive-local point -> module-local, the same transform as placePart) so an anchor read off a primitive's own geometry (the coffee station's strip light) can never drift from it. Both the spine-door and the side-door openings are sealed by the room's own socket-centred hatches (fillsSocket) \u2014 the M2-T6 spine still brings the shaft + per-deck sockets and NO hatch. CROSS-TASK: M2-T4 (ops) has the same off-centre side-door shape and can copy galley.ts wholesale. NEXT: M2-T4 (author the ops module \u2014 airlock + suit locker, workbench, med bay; dependency M2-T1 met)."
    },
    "M2-T4": {
      "status": "complete",
      "date": "2026-09-14",
      "title": "Author ops module (airlock + suit locker, workbench, med bay bay)",
      "files_modified": [
        "src/kit/types.ts",
        "src/kit/parts.ts",
        "src/kit/parts.test.ts",
        "src/kit/catalog.ts",
        "src/kit/catalog.test.ts",
        "src/kit/render/primitives.tsx",
        "src/kit/modules/ops.ts",
        "src/kit/modules/ops.test.ts",
        "src/kit/modules/index.ts",
        "src/kit/modules/registry.ts",
        "src/kit/modules/registry.test.ts",
        "src/kit/modules/render/OpsModule.tsx",
        "src/kit/modules/render/index.ts"
      ],
      "tests_added": [
        "src/kit/modules/ops.test.ts \u2014 20 tests: module integrity clean + gate throws nothing; contract footprint 3.9 x 3.0 x 4.6 with an ops/airlock/med-bay label; CONTRACT_KIT diff empty and both door-socket origins equal contractSocketOrigins() (spine-door [-z] + side-door [-x], position and facing); exactly one standardized spine door flush in the -z face; the contract side door in the -x face 1.0 m AFT along the wall; shell (deck plate + 4 bulkheads) builds tight to the declared box {min [-1.95,-0.2,-2.3], max [1.95,3,2.3]}; the port wall is CUT at the side-door origin (piers at z [-2.3,-1.45] and [-0.55,2.3], opening centre -1.0 / width 0.9, lintel from the 2.0 m door head); the airlock vestibule is walled off aft-port by two partitions whose bounds put its interior at x [-1.85,-0.9] / z [-2.2,-0.45] with the port wall as its outer side; the airlock is entered through a 0.8 x 1.9 m interior doorway whose prism no other assembly occupies, with the interior hatch socket-centred on it (leaf exactly 0.78 x 1.88, frame straddling the partition, head at the door top); the suit rack hangs two vac suits in equal bays on the vestibule\u2019s aft wall facing the entry (7 parts = board + 2 x (torso/helmet/visor), symmetric about the centreline, visors proud of the wall, webbing reserved to it alone); the tool wall is bolted to the airlock partition\u2019s room face (doors proud into the room, 1.2 m spanning the partition); the workbench runs along the starboard wall with its terminal above it and the task socket COINCIDENT with the fixture\u2019s own emissive lens (placePoint); the med bay (exam bed = couch with no straps, headboard on the bow wall; med cabinet on the bow wall; med monitor over the bed) stays clear of the bench; 11 equipment anchors equal their assemblies\u2019 placements; practical lighting only (4 ceiling panel sockets each over a real housing incl. the airlock\u2019s own lamp, one task socket, two screen sockets); material slots are exactly the seven the room draws; both door-socket prisms are clear of solid geometry; the centreline walk from the spine door to the bow wall is clear of everything but the deck plate; the collision hint is the derived one-box-per-solid-part hull (39) with all three hatches excluded; every fixture stays inside the module box",
        "src/kit/parts.test.ts \u2014 2 new tests (44 total): the vac-suit rack hangs one suit per bay (board stands on y = 0, torso is webbing proud of the board at the hook height, helmet is a bulkhead barrel centred on the torso\u2019s top edge and inside the board, visor is a proud screen-slot disc, suits sit symmetric in their bays and never widen the rack); rejects a suit wider than its bay, a suited head poking above the board, a zero suit count and a negative hook height",
        "src/kit/catalog.test.ts \u2014 updated the canonical primitive list (suit-rack before heat-shield) and the prop-category count (6); the existing bounds/slots pins now cover the new primitive",
        "src/kit/modules/registry.test.ts \u2014 updated the registry id-list expectations to [head, galley, ops] and added the getAuthoredModule(\u2018ops\u2019) lookup"
      ],
      "verification": "npm run verify green end-to-end: oxlint 0 warnings/0 errors (82 files, 104 rules); prettier --check clean repo-wide (3 touched files formatted via prettier --write); tsc -b --noEmit clean; vitest 285 passed / 22 files (was 263/21 \u2014 22 new); npm run build green (check:slots 9/9 assigned, tsc, vite built in 364ms, only the pre-existing >500 kB chunk warning)",
      "notes": "DONE 2026-09-14: M2-T4 \u2014 authored the OPS module in src/kit/modules/ops.ts: 26 assemblies -> 90 kit parts (all from the M2-T1 builders, rule 8) on the contract footprint 3.9 x 3.0 x 4.6. Layout read from the spine door: the AIRLOCK is a walled-off vestibule in the aft-port corner (forward partition at z = -0.4 with the 0.8 x 1.9 m interior doorway + socket-centred interior hatch; starboard partition running fore-and-aft at x = -0.85), its outer door IS the contract side-door in the port face (cut with bulkheadParts door.centerX, sealed by the room\u2019s own fillsSocket hatch), the two VAC SUITS hang on a rack at its aft end facing the entry, and the TOOL WALL (2-door tool cabinet bank) is bolted to the partition\u2019s room face right beside it; the WORKBENCH runs along the starboard wall amidships with a terminal screen and its own wall-recessed task strip light; the MED BAY fills the bow-starboard corner (fold-down exam bed = the couch primitive with no straps, headboard on the bow wall; med cabinet on the bow wall; med monitor above the bed); the centreline walk stays clear from the spine door to the bow wall. 7 light sockets (4 panel / 1 task / 2 screen) and 11 equipment anchors derived from the placements; collision hint = one box per solid part (39). Door sockets reproduce CONTRACT_KIT exactly (spine-door [0, 1.0, -2.3] -z; side-door [-1.95, 1.0, -1.0] -x) -> moduleContractProblems(OPS_MODULE, CONTRACT_KIT) == []. KIT EXTENSION (additive; M2-T5/T6 may use it): new primitive \u2018suit-rack\u2019 \u2014 suitRackParts (params width/height/depth/suits/suitWidth/suitHeight/suitDepth/suitY/helmetRadius; emits a bulkhead rack board + per suit a webbing torso, a bulkhead helmet barrel centred on the torso\u2019s top edge and a proud screen-slot visor disc; rejects a suit wider than its bay, a head above the board, a bad suit count / hook height) + the KIT_PRIMITIVES entry (bounds {min [-0.45,0,-0.04], max [0.45,2,0.39]}, slots bulkhead/screen/webbing, category prop) + the <SuitRack> R3F component \u2014 because PRD \u00a74\u2019s airlock landmark is \u201ctwo vac suits on racks\u201d and no existing primitive could express a suit (rule 8: extend the builder, never hand-place). Precedent followed: the M2-T3 off-centre door.centerX + placePoint are reused (the port side door is off-centre and the bench task socket is read off the fixture\u2019s own lens via placePoint). CROSS-TASK: the room owns both socket hatches (fillsSocket) \u2014 M2-T6 still brings the shaft + per-deck sockets and NO hatch; the interior airlock hatch is deliberately NOT fillsSocket (it fills an interior opening, not a DoorSocket) and is excluded from the hull like the socket hatches; the corridor test (x +/-0.35, y 0.1-2.0, z +/-2.15 clear of everything but the deck plate) is the M0-T1 wayfinding read at spec level. NEXT: M2-T5 (author the engineering module \u2014 reactor access, drive glow window, radiation striping \u2014 plus the long-haul storage module; dependencies M2-T1 met)."
    },
    "M2-T5": {
      "status": "complete",
      "date": "2026-09-15",
      "title": "Author engineering module (reactor access, drive glow window, radiation striping) + storage module (long-haul variant)",
      "files_modified": [
        "src/kit/parts.ts",
        "src/kit/parts.test.ts",
        "src/kit/types.ts",
        "src/kit/catalog.ts",
        "src/kit/catalog.test.ts",
        "src/kit/render/primitives.tsx",
        "src/kit/modules/engineering.ts",
        "src/kit/modules/engineering.test.ts",
        "src/kit/modules/storage.ts",
        "src/kit/modules/storage.test.ts",
        "src/kit/modules/registry.ts",
        "src/kit/modules/registry.test.ts",
        "src/kit/modules/index.ts",
        "src/kit/modules/render/EngineeringModule.tsx",
        "src/kit/modules/render/StorageModule.tsx",
        "src/kit/modules/render/index.ts"
      ],
      "tests_added": [
        "src/kit/parts.test.ts - 6 new tests (49 total) for the three M2-T5 kit builders: glow window (4 frame rails/jambs bound the exact opening, the drive glow RECESSED inside it, 5 grating bars proud of the frame's +Z face at the primitive's own 120-degree-free spacing, tight bounds {min [-0.7,-0.45,-0.06], max [0.7,0.45,0.09]}; unshielded window with 0 bars; rejects a frame with no opening, bars wider than their pitch, glowInset 0.5, a negative bar count, a non-positive depth); radiation sign (hazard plate + hub + 3 blades proud of the plate, blade angles measured from the part positions = 90/210/330 deg, all at bladeDistance, plate sets the bounds {min [-0.2,-0.25,-0.01], max [0.2,0.25,0.022]}; rejects a mark that does not fit inside its placard / a non-positive markThickness); cargo crate (2 skids standing on y = 0 inset under the depth, the body ON the skids, one strap per bay as a lid band + two end bands in the webbing slot, a hazard placard thinner than the straps on the +Z face, tight bounds {min [-0.45,0,-0.62], max [0.45,0.78,0.62]}; rejects a strap wider than its bay, skids thicker than half the depth, zero straps, a placard that cannot fit).",
        "src/kit/modules/engineering.test.ts - 23 tests: module integrity clean + gate throws nothing; contract footprint 4.5 x 3.0 x 4.8 with an engineering/reactor/drive-glow label; CONTRACT_KIT diff empty with both door-socket origins equal contractSocketOrigins() (spine-door + high-hatch, position and facing) INCLUDING the contract's non-standard opening; exactly one standardized spine door flush in the -z face at [0, 1.0, -2.4]; the high-hatch is the kit's ONE non-standard door (position [2.25, 1.2, 0.6] +x, door 0.8 x 1.6, doorCenterDeckOffsetM = +0.2, the only odd centre AND the only odd opening); shell (deck plate + 4 walls) builds TIGHT to the declared box {min [-2.25,-0.2,-2.4], max [2.25,3,2.4]}; the starboard wall is CUT at the socket origin (piers z [-2.4,0.2] and [1.0,2.4], opening centre 0.6 width 0.8, sill 0..0.4 under it, lintel from the 2.0 m head); a socket-centred hatch sits on each door at that socket's own opening size (spine leaf 0.88 wide; high-hatch leaf 0.78 x 1.58 at the 1.2 m centre, yaw 3); the reactor bulkhead walls off the bow (full-width partition at z = 1.4, bounds {min [-2.25,0,1.35], max [2.25,3,1.45]}) with a 0.8 x 1.6 m WALK-THROUGH access doorway (no sill - the deck is the threshold; lintel underside at 1.6) that its own interior hatch seals while no other assembly enters the prism; the drive glow window is set in that bulkhead (placement [0.9,1.55,1.29] yaw 2, frame back ON the bulkhead face, glow RECESSED between the frame faces at [0.3368,1.2068,1.266]-[1.4632,1.8932,1.314], five conduit grating bars proud at z [1.2,1.23] spread across the 1.28 m opening); the radiation placard hangs port of the window, starboard of the door (hazard plate [0.4 x 0.5 x 0.02], hub + 3 blades entirely AFT of the plate, blade angles 90/210/330); ceramic heat shielding clads the bulkhead either side (ceramic plate + inset hazard stripe, bounds {min [0.1,0.15,1.301], max [1.7,0.85,1.349]} and its port twin, both under the window and clear of the door); the machinery bay (stores locker flush on the port wall with doors proud into the room; spares locker; starboard machinery locker with its door face turned into the room; console bench aft of the high-hatch) blocks neither doorway; the console carries a display plus a wall-recessed task strip whose socket IS the fixture's own emissive lens (placePoint); the room's `reactor` light socket is the window's OWN glow lens; 13 equipment anchors equal their assemblies' placements; practical lighting only (3 panel sockets each over a real housing + 1 task + 1 screen + 1 reactor); material slots exactly deckplate/bulkhead/conduit/panel-light/screen/hazard/ceramic; both door prisms plus the reactor access prism are clear of solid geometry; the collision hint is the derived one-box-per-solid-part hull with all three hatches excluded; every fixture stays inside the module box; the compartment landmark check (window starboard of the doorway, placard further starboard, 1.6 m of headroom over the gap).",
        "src/kit/modules/storage.test.ts - 19 tests: module integrity clean + gate throws nothing; contract footprint 4.0 x 3.0 x 6.0 (the kit's deepest room, > 1.4x its width) labelled storage/cargo/long-haul; CONTRACT_KIT diff empty with both door-socket origins equal contractSocketOrigins() (spine-door -z + side-door +x); exactly one standardized spine door flush in the -z face at [0, 1.0, -3.0]; the contract side door in the +x face 1.0 m bow-ward of the wall centre; shell (deck plate + 4 walls) tight to the box {min [-2,-0.2,-3], max [2,3,3]}; the starboard wall is CUT at the side-door origin (piers z [-3,0.55] and [1.45,3], opening centre 1.0 width 0.9, lintel from the 2.0 m head); socket-centred hatches on both doors (side leaf 0.88 x 1.98 yaw 3); four LASHED CRATE STACKS two crates high (10 parts each: 2 skids + body + 2 straps x 3 + placard, 6 webbing parts, envelopes {x +/-0.45, z +/-0.62}, the upper crate resting exactly on the lower one's lid strap, long axis fore-and-aft); the centreline lane (1.2 m wide, the PRD's two-person corridor) stays clear from the aft doorway to the bow wall above the deck's cable ducts; the spares rack lines the port wall forward with doors out into the hold; the loadmaster station (bench on the bow wall, terminal on the wall above it, task strip socket COINCIDENT with the fixture's own lens, nothing blocking the side door it stands beside); 14 equipment anchors equal their assemblies' placements; practical lighting only (4 panel sockets spread the length of the hold, each over a real housing, plus 1 task + 1 screen); material slots exactly deckplate/bulkhead/conduit/panel-light/screen/hazard/webbing and NOT the galley's coffee-accent; both door prisms clear of solid geometry; collision hint = derived one-box-per-solid-part hull with both hatches excluded; every fixture inside the module box; the cargo check (both columns flush on their walls, the forward starboard stack stood inboard of the side-door sweep).",
        "src/kit/catalog.test.ts - updated the canonical primitive list (glow-window, radiation-sign, cargo-crate after heat-shield), the prop-category count (7) and the thermal category list (heat-shield + glow-window + radiation-sign); the existing bounds/materialSlots pins now cover the three new primitives. src/kit/modules/registry.test.ts - registry id-list expectation is now all five room modules (head, galley, ops, engineering, storage) with the engineering/storage lookups added."
      ],
      "verification": "npm run verify green end-to-end: oxlint 0 warnings/0 errors (88 files, 104 rules); prettier --check clean repo-wide (6 touched files formatted via prettier --write); tsc -b --noEmit clean; vitest 334 passed / 24 files (was 285/22 - 49 new); npm run build green (check:slots 9/9 assigned, tsc, vite built in 339ms, only the pre-existing >500 kB chunk warning).",
      "notes": "DONE 2026-09-15: M2-T5 - authored BOTH remaining room modules. (1) ENGINEERING in src/kit/modules/engineering.ts: 26 assemblies -> 97 kit parts on the contract footprint 4.5 x 3.0 x 4.8, read from the spine door: the aft two-thirds are the machinery bay (stores locker bank on the port wall, spares locker forward of it, starboard machinery locker + console bench with display and wall-recessed task strip), and the bow end is walled off by a full-width REACTOR BULKHEAD at z = 1.4 carrying a 0.8 x 1.6 m walk-through access doorway sealed by its own interior hatch (hatch-reactor-access, not fillsSocket). The PRD section 4 reactor-room landmark is authored on that bulkhead's room face: the DRIVE GLOW WINDOW (new primitive) - a shielded frame with the drive's glow RECESSED inside it and five grating bars proud into the room (the room's `reactor` light socket is anchored on the window's OWN glow lens via placePoint, kind 'reactor' - the LightKind that had no owner until now); the RADIATION PLACARD (new primitive) port of the window with its three-fold mark proud of the plate; CERAMIC HEAT SHIELDING with worn hazard stripes (the M2-T1 heat-shield primitive, finally placed) either side of the window. 6 light sockets (3 panel / 1 task / 1 screen / 1 reactor) and 13 equipment anchors derived from the placements; collision hint = one box per solid part (51). The starboard wall carries the contract's NON-STANDARD high-hatch (position [2.25, 1.2, 0.6] +x, door 0.8 x 1.6, deck offset +0.2) cut by bulkheadParts door.centerX/centerY -> moduleContractProblems(ENGINEERING_MODULE, CONTRACT_KIT) == []; it is the kit's only non-standard door and the wall-cut + hatch-fill path is now exercised end to end. (2) STORAGE in src/kit/modules/storage.ts: 27 assemblies -> 150 kit parts (8 lashed crates + the rack/bench/terminal/lighting/shell assemblies) on the contract footprint 4.0 x 3.0 x 6.0 (the kit's deepest room, the long-haul stretch): four cargo stacks lashed two high (two per wall) with the new CARGO CRATE primitive (skids + webbing tie-downs over the lid and down both ends + a hazard placard), a spares rack on the port wall forward, the loadmaster's station (bench + terminal + task strip) in the bow-starboard corner, and a clear 1.2 m centreline lane the length of the hold. 6 light sockets (4 panel / 1 task / 1 screen) and 14 equipment anchors; collision hint = one box per solid part (102). Door sockets reproduce CONTRACT_KIT exactly (spine-door [0, 1.0, -3.0] -z; side-door [2.0, 1.0, 1.0] +x). KIT EXTENSION (additive; M2-T6 may use it): three new M2-T1 primitives - `glow-window` (glowWindowParts: width/height/depth/frameWidth/glowInset/barCount/barWidth/barThickness; bulkhead frame + RECESSED panel-light glow + conduit grating bars proud of the +Z face; rejects a frame with no opening, bars wider than their pitch, glowInset >= 0.5, a negative bar count), `radiation-sign` (radiationSignParts: width/height/thickness/hubRadius/bladeRadius/bladeDistance/markThickness; hazard plate + hub and three DISC blades at the mark's own 90/210/330 deg positions - the kit's boxes-and-cylinders vocabulary cannot cut the symbol's wedges, so the blades are discs, same three-fold read; the plate always sets the bounds, enforced by the markReach fit test), `cargo-crate` (cargoCrateParts: width/height/depth/skidHeight/skidThickness/straps/strapWidth/strapThickness; two conduit skids + bulkhead body + per-bay webbing lid band and two end bands + a hazard placard thinner than the straps so it never beats them for the bounds) - wired through KIT_PRIMITIVES (bounds/slots gated by catalog.test.ts), PrimitiveId, defaultPrimitiveParts and the R3F primitive components (GlowWindow/RadiationSign/CargoCrate). CROSS-TASK: the M2-T6 spine still supplies the shaft + per-deck sockets and NO hatch (every room owns its hatches); M2-T7's authoredKitProblems(AUTHORED_MODULES, CONTRACT_KIT) now covers all five room modules and is clean, so the M2-T7 harness has nothing left to author before it can run. NEXT: M2-T6 (spine module - vertical ladder/crawl shaft with per-deck door sockets), which is the last module task and completes M2."
    },
    "M2-T6": {
      "status": "complete",
      "date": "2026-09-16",
      "title": "Author spine module (vertical ladder/crawl shaft with per-deck door sockets)",
      "files_modified": [
        "src/kit/parts.ts",
        "src/kit/parts.test.ts",
        "src/kit/modules/types.ts",
        "src/kit/modules/integrity.ts",
        "src/kit/modules/spine.ts",
        "src/kit/modules/spine.test.ts",
        "src/kit/modules/registry.ts",
        "src/kit/modules/registry.test.ts",
        "src/kit/modules/index.ts",
        "src/kit/modules/render/SpineModule.tsx",
        "src/kit/modules/render/index.ts"
      ],
      "tests_added": [
        "src/kit/modules/spine.test.ts - 21 tests: band integrity clean + gate throws nothing + contract diff empty (the shaft has no fixture-time contract entry) + authoredKitProblems(AUTHORED_MODULES, CONTRACT_KIT) clean; dims 1.4 x 3.2 x 1.4 with shaft merged + label naming the trunk; the band tiles the column (deckFloorYFor(i+1) + 3.2 === deckFloorYFor(i)) and sits on the fixture layout's SPINE_HALF_M column; four face doorways ('+z'/'-z'/'+x'/'-x') each flush at +/-0.7 on its own face at the standard 1.0 m centre with the standard 0.9x2.0 opening and no spine-door; CROSS-PIN against the validator's synthesized per-deck band (spineBandDoors: id/centre/facing/opening all equal, same order) and against SHAFT_FACES; shell (plate + four walls) tight to {min [-0.7,-0.2,-0.7], max [0.7,3,0.7]}; each wall seated flush inside its own face (four exact boxes); each wall CUT at its own socket (3 parts: piers 0.25 m at x +/-0.575, lintel 0.9 x 1.0 at y 2.5, opening edges == the socket's door); all four doorway prisms clear of EVERY part (the band authors no hatch/fillsSocket, and draws no hazard slot), and the four prisms pairwise disjoint; the ladder is 2 rails along y of length 3.2 at x +/-0.195 + 10 rungs along x at 0.3..3.0 (spineRungHeights) with the run reaching exactly the band box top (the deck above's floor); the stacked pair's footholds (both deck floors + both bands' rungs) never gap more than one rung pitch (0.2 step onto the deck, then 0.3); the band below's ladder passes through the crawl opening without touching a plate strip, its rails land on the deck above's floor and the upper band's plate underside == the lower band's walls' top (trunk continuity); the crawl opening is a 0.7 x 0.7 hole framed by 4 exact strips (solid area == 1.4^2 - 0.7^2) and contains the ladder with margin; practical lighting only (2 panel sockets over real housings, above every doorway head, clear of the ladder, wall faces and all four prisms); 4 equipment anchors equal their assemblies' placements; material slots exactly deckplate/bulkhead/conduit/panel-light; cable trees are non-solid vertical runs in the free diagonal corners (clear of all four prisms, inside the wall faces, spanning the band's clear height); every assembly inside the declared box with the deck plate the only sub-floor geometry and the ladder the only run in the box's top 0.2 m; collision hint == the derived one-box-per-solid-part hull (28 boxes: 4 plate strips + 4 walls x 3 + 12 ladder parts, ladder included as the climb surface, cables/lights excluded); shaft doorway rules reject a missing face doorway (found 0), a duplicated face (found 2), a mis-faced doorway, a +0.2 m stepped centre (200.0 mm off standard), a non-standard opening (0.8 x 1.6) and a stray spine-door; an unflagged band falls back to the room rule (needs exactly one spine-door).",
        "src/kit/parts.test.ts - 6 new tests (54 total): the deck-plate crawl opening is a closed frame of four DECKPLATE strips (-x/+x full depth, then the -z/+z end strips) with exact sizes/positions, the full plate footprint kept (walking surface still at y = 0), solid area == plate - hole, and no -0 leaking out of a zero-centred opening; an off-centre opening carries the frame with it (all four strips re-derived); rejections: an opening as large as the plate or one pushed off its edge (either axis) leaves no plate, a non-positive opening extent, non-finite centreX/centreZ, and cable runs across a crawl opening; the solid-plate build is unchanged (no opening / opening: undefined are bit-identical)."
      ],
      "verification": "npm run verify green end-to-end: oxlint 0 warnings/0 errors (91 files, 104 rules); prettier --check clean repo-wide (4 files formatted via prettier --write); tsc -b --noEmit clean; vitest 360 passed / 25 files (was 334/24 - 27 new); npm run build green (check:slots 9/9 assigned, vite built in 337ms, only the pre-existing >500 kB chunk warning).",
      "notes": "DONE 2026-09-16: M2-T6 - authored the SPINE module in src/kit/modules/spine.ts: the vertical ladder/crawl trunk as a per-deck BAND (dims 1.4 x 3.2 x 1.4, floor at local y = 0, AuthoredModule.shaft = true), because a module type has fixed dimensions and ships carry 5-6+ decks: dimensions[1] === DECK_PITCH_M is exactly the statement that consecutive bands tile the column floor-to-floor (deckFloorYFor(i+1) + 3.2 === deckFloorYFor(i)), which is how the M3-T1 assembler will 'generate the spine run' (one band per deck, at the deck origin). FOUR face doorways, ids '+z'/'-z'/'+x'/'+x' (SHAFT_FACES), one per face, flush at +/-SPINE_SHAFT_HALF_M = 0.7 with the standard 1.0 m centre and standard 0.9 x 2.0 opening. They are pinned 1:1 to the sockets the M1-T3 validator synthesizes per deck (spineBandDoors) and to the fixtures' SPINE_HALF_M column, so a room that passes validation lands on a doorway that really exists; the band's own contract-kit diff is empty by design (the shaft has no fixture-time entry - the band is implicit). Each face wall is cut by bulkheadParts AT its own socket (opening width/height/centre read from the DoorSocket, rule 8), so the hole and the socket can never drift. The deck plate is a closed frame around a 0.7 x 0.7 CRAWL OPENING on the shaft axis: the band's plate IS the ceiling of the band below (the subfloor slab of its own deck), which is why the hole lives here and not in the band the ladder starts from. The LADDER is one full deck pitch tall (rails 0 -> 3.2, ten rungs on a 0.3 m pitch from 0.3 to 3.0), so a climb across a deck boundary has a maximum foothold gap of one rung pitch (0.2 m step onto the deck floor, then 0.3 m) - asserted on a stacked pair, and it is the only geometry in the band's top 0.2 m, which is why the band's box is a full pitch tall rather than one deck clear height. Practical lighting only: 2 recessed panel lights above the doorway heads; 2 vertical cable trees (service routing) in the only free volume outside the four doorway prisms and the ladder; 4 equipment anchors (ladder, deck-plate, both trees); collision hint derived (28 boxes = 4 plate strips + 4 walls x 3 parts + 12 ladder parts). The band authors NO hatch: the ROOM owns the hatch on its own spine-door socket (M2-T2 cross-task seam) - every part is checked clear of all four doorway prisms. KIT EXTENSION (M3/M4 may use it): deckPlateParts gained 'opening' (a crawl hole cut as a closed frame of four strips, solid area = W*D - w*d, mutually exclusive with cableRuns since the ducts run the full width; mirrors bulkheadParts' doorway cut). integrity.ts: AuthoredModule.shaft flag + shaftDoorProblems (one doorway per face, id == face, standard centre and opening, no spine-door, no non-face doorway id); the room rules are untouched - an unflagged band still demands exactly one spine-door (negative cases pinned in spine.test.ts). CROSS-TASK: (1) an unjoined face socket stays legal (the validator's blanking rule) - M3-T2 generates the blanking sleeve from the socket, the band does not carry four hatch leaves; (2) M3-T3 consumes moduleCollisionBoxes (the ladder IS a hull; the cables and lights are not); (3) M3-T5 can climb spineRungHeights() rather than re-deriving the rungs; (4) the topmost band's run ends at the structure plane (y = 3.2 = the same slab every deck's plate occupies), so no band-specific capping is needed. NEXT: M2-T7 (kit test harness - each module renders standalone, material slots fully assigned, door sockets at spec'd origins; deps M2-T2..T6, now all met). Held human spikes still open for an interactive session: M0-T1 (wayfinding walk), M0-T3 (galley lighting look), M0-T4 (ladder-climb feel) - unchecked by design, blocking nothing."
    },
    "M2-T7": {
      "status": "complete",
      "date": "2026-09-17",
      "title": "Kit test harness: each module renders standalone, material slots fully assigned, door sockets at spec'd origins",
      "files_modified": [
        "src/kit/harness/renderTree.ts",
        "src/kit/harness/components.ts",
        "src/kit/harness/StandaloneModuleScene.tsx",
        "src/kit/harness/checks.ts",
        "src/kit/harness/harness.ts",
        "src/kit/harness/index.ts",
        "src/kit/harness/harness.test.tsx",
        "src/kit/render/SlotMaterial.tsx",
        "src/materials/theme.ts",
        "src/invariants/registry.ts",
        "src/invariants/checks.ts",
        "src/invariants/index.ts",
        "src/invariants/invariants.test.ts",
        "src/kit/index.ts",
        "package.json",
        "ci/github-actions-ci.yml",
        "README.md"
      ],
      "tests_added": [
        "src/kit/harness/harness.test.tsx \u2014 38 tests in four groups. GATE ON THE AUTHORED KIT (11): clean report (passed, no problems, 6 modules all pass, ids in build order); one mesh per part per module (totals.meshes == totals.parts == 546; per module parts == moduleParts(module).length); the union of drawn slots is the whole 9-slot \u00a74 vocabulary; every contract door socket at its spec'd origin with 0.000 mm residual on every axis (galley side-door row pinned); the band's four face doorways reported contract-free (the shaft has no fixture-time entry); every module has >=1 light + >=1 equipment anchor + >=1 door; the one-line summary; every module type the four canonical fixtures reference resolves in the authored kit; determinism (two runs equal); assertKitHarnessClean returns clean + throws on a broken kit; socketOriginIndex(CONTRACT_KIT) === contractSocketOrigins() (cross-pin, no fork). INJECTED DEFECTS (11): dimension drift vs contract; a socket 0.6 mm off its origin (problem names the socket + residual measured as 0.6 mm); a missing contract socket; an extra socket; a slot the geometry draws that the theme leaves unassigned (blank payload) while the clean theme passes; an incomplete theme as a kit-level problem; a module with no standalone render component (module + kit level); a component that draws another module's geometry (99 meshes for 70 parts); a component that draws nothing (0 for 90); a mesh shifted 0.5 m off the part it claims to draw; a duplicated module id + a missing contract module; and 'never throws on a broken kit \u2014 problems are data'. HEADLESS RENDER MODEL (6): every authored module's render tree == expectedRenderedParts(moduleParts) 1:1; the \u00a74 slot is carried on every material and read back; module placement lands on the group not the meshes; the walker rejects non-kit hosts (<sphere>) and a mesh without exactly one geometry+material (0/2 geometries, 0 materials) and a material with no slot name; describeRenderedPart wording. STANDALONE SCENE / REAL DOM (6): moduleComponentFor lookup + loud unknown-id failure listing known ids; StandaloneModuleScene draws exactly one module's meshes (and throws on an unknown module id); EVERY module mounted alone in a real react-dom render with one mesh per part and geometry/slot/transform equal to the part list (only react-dom's R3F-intrinsic warnings tolerated, everything else in console.error = failure); the deck-local pose lands on the module root group; exactly one <group> root with all meshes under it; every rendered geometry extent positive (no zero-sized geometry reaches the DOM). REPORTING (3): the render check's mesh count == the module's part count and == the report's meshes; residuals measured per axis for every socket of every module; unconstrained (non-contract) sockets report 0/unconstrained.",
        "src/invariants/invariants.test.ts \u2014 5 new tests (39 total) for room-lit now being LIVE: passes on all four canonical fixtures (incl. the stress rig \u2014 its defects are geometry, not lighting) with the authored-kit wording; tallies count spec refs + the implicit per-deck shaft band (bands == deck count, fixtures > room refs); fails a spec referencing a module type the kit does not know ('cargo' \u2192 'not in the kit'); fails a spec with no module instances at all; and \u2014 with a kit injected \u2014 fails a dark module AND a dark shaft band while the canonical kit lights the same spec cleanly. Updated pins: live ids now [hatch-alignment, spine-connectivity, spawn-inside, room-lit], stubs [seams-watertight, collision-match]; DEFERRED_ROW \u2192 REAL_SHIP_ROW with the last column 'pass'; the stress row's last column 'pass'; the deferred-owners map drops room-lit."
      ],
      "verification": "npm run verify green end-to-end: oxlint 'Found 0 warnings and 0 errors' (98 files, 104 rules); prettier --check clean repo-wide (6 touched files formatted via prettier --write); tsc -b --noEmit clean; vitest 403 passed / 26 files (was 360/25 \u2014 43 new); npm run build green (check:slots 9/9 assigned, tsc, vite built in 342ms; only the pre-existing >500 kB chunk warning). New gate command `npm run check:kit` (vitest run src/kit/harness) \u2014 38 passed.",
      "notes": "DONE 2026-09-17: M2-T7 \u2014 the KIT TEST HARNESS, and the M2 machine gate as executable data. src/kit/harness/ (6 modules + tests): (1) renderTree.ts is the headless render model \u2014 jsdom has no WebGL, so instead of mounting a <Canvas> the harness WALKS the React element tree a module component returns and extracts one item per <mesh> (geometry, \u00a74 slot, position, rotation). The walk resolves function components by CALLING them (the kit's render layer is hookless by construction) and rejects anything the kit's vocabulary does not allow: a stray host element, a mesh without exactly one geometry + one material, a cone where the kit draws uniform cylinders, a material with no slot name. It is node-loadable (no DOM) yet pinned against a REAL react-dom render of the same components (same mesh count, same geometry args, same slot names, same transforms) so the model can never silently drift from what React does. (2) components.ts = the standalone render surface (MODULE_COMPONENTS: one component per authored module + moduleComponentFor/moduleSceneElement); StandaloneModuleScene.tsx mounts exactly ONE module by id at a deck-local pose. (3) checks.ts = the four pure rule groups: slots (themeAssignsSlot per drawn slot \u2014 the M1-T4 rule asked per module, newly exported from src/materials/theme.ts so it is NEVER forked), render (rendered items vs the module's own parts 1:1), sockets (per-axis mm residuals against the contract kit's origin index; the pass/fail decision stays with M2-T2 moduleContractProblems), and the render-component lookup. (4) harness.ts = runModuleHarness / runKitHarness / assertKitHarnessClean \u2014 NEVER throws on the authored kit (problems are data, same posture as the M0-T6 harness and the M1-T3 validator), reporting per module: integrity, contract, slots, render, plus parts/meshes/slots/light+equipment anchors/door-socket residual rows, and kit-level rules (unique ids, every contract module authored so a spec ref resolves, a component for every module, the M1-T4 theme gate). VERDICT on the authored kit: CLEAN \u2014 6 modules, 546 parts = 546 meshes drawn, all 9 \u00a74 slots assigned in theme 'firebrand', 9/9 contract door sockets at 0.000 mm residual (13 doorways total incl. the band's four synthesized faces), 35 light + 62 equipment anchors. M2 MACHINE GATE accordingly met: every module compiles (tsc), renders in harness, fills all material slots, socket origins match the contract. ALSO IN THIS TASK: (a) SlotMaterial now names its material after the \u00a74 slot (three.js material.name) \u2014 that is how the harness reads the slot back off the render tree, and it is what the M6 export contract wants (named materials); no shading change. (b) \u00a78 bullet 6 'room-lit' is now LIVE (its declared owner WAS M2-T7): checkRoomLit counts light fixtures per module INSTANCE \u2014 every spec ref PLUS the implicit per-deck shaft band the assembler synthesizes (M2-T6) \u2014 over the AUTHORED kit (the fixture-time CONTRACT_KIT predates light sockets and carries none), failing on an unknown module type (fixtures unknown), a dark room, a dark shaft band, or a spec with no instances. All four canonical fixtures PASS room-lit (incl. the stress rig: its declared defects are geometry/socket, not lighting), so the harness rows are now [deferred, pass, pass, deferred, pass, pass] for the three real ships and [deferred, fail, fail, deferred, fail, pass] for the stress rig \u2014 4 of 6 \u00a78 bullets live, 2 stubs left (seams-watertight M3-T2, collision-match M3-T3). (c) `npm run check:kit` is the one-command gate (also a step in the CI static job at ci/github-actions-ci.yml); README documents the harness. CROSS-TASK SEAMS for M3: runKitHarness(AUTHORED_MODULES, CONTRACT_KIT) is what M3-T1 should call before assembling (the authored kit reproduces the contract the M0-T5 ships were authored against); moduleCollisionBoxes() is the M3-T3 hull input; the harness's render model is the reference for the M3-T7 merge/instance pass (per-part meshes in, merged geometry out); moduleRenderProblems is the guard that a module component can never draw geometry its own part list does not describe. NOTE: switching the M1-T3 socket checks from CONTRACT_KIT to AUTHORED_KIT is deliberately NOT done here \u2014 those checks resolve the numbers the fixtures were authored with, and the harness gates that the two kits agree (0.000 mm); M3-T1 flips the assembler to AUTHORED_KIT."
    },
    "M3-T1": {
      "status": "complete",
      "date": "2026-09-18",
      "title": "Assembler: stack decks from spec, join modules at door sockets, generate spine run; emit merged per-deck geometry + instanced kit batches",
      "files_modified": [
        "src/assembler/types.ts",
        "src/assembler/place.ts",
        "src/assembler/batches.ts",
        "src/assembler/spineRun.ts",
        "src/assembler/joins.ts",
        "src/assembler/assemble.ts",
        "src/assembler/checks.ts",
        "src/assembler/index.ts",
        "src/assembler/assembler.test.ts",
        "src/validation/sockets.ts"
      ],
      "tests_added": [
        "src/assembler/assembler.test.ts \u2014 32 tests in six groups. DECK STACKING (6): one node per spec deck in spec order at the spec floors (0 \u2026 \u221212.8) and the spec's label; ship identity echoed; every ref instantiated at the authored offset + yaw through the shared resolver (moduleOrigin \u2014 re-derived nothing) with moduleParts-length part lists; exactly one synthesized shaft band per deck at [0, floorY, 0] rot 0 (source moduleIndex \u22121, and no spec ref names 'spine'); the run tiles (band i's top + pitch \u2261 the floor above, spineRunProblems [] and a 50 mm break caught); the band is one full pitch tall (\u22120.2 plate slab \u2192 floorY + 3.2, \u00b10.7 column). JOINS (6): one aligned spine join per room (normal/lateral/vertical 0.0 mm; band socket is the reference, the room door the other side), no module-to-module mates on the canonical ships, blanks + 2\u00d7joins == every socket (3 band faces + every room side door blanked), a two-room deck joining two band faces (+z and \u2212z, clean), the QA rig's 200 mm high-hatch\u2194side-door mate reported as an unaligned module join (and its off-spine room reported as off the run), and a 50 mm floor step surfacing in the run check. GEOMETRY + INSTANCES (8): the two emitted halves are a PARTITION of all four fixtures' parts (grouped + batched == placed, disjoint and complete, node mirrors the plan), groups one per \u00a74 slot in MATERIAL_SLOTS order with deck-scoped ids and real sources, batches \u2265 2 instances of one mould/slot with the shape and the pieceId pinned, EVERY batch instance reconstructed back to its world part (placePart(mouldOf(shape), placementFor(...)) toEqual the part \u2014 the mould/instancing contract), the shaft's ten ladder rungs as one cylinder batch at 0.3\u20263.0 m, a higher minInstances re-partitioning with strictly fewer draw calls and no lost part, determinism (two runs deep-equal; a lone assembleDeck equals the ship's copy), and partitionParts agreeing with the ship's own partition. INTERACTIVES (5): a door per room door socket at the socket center yawed to its facing (9 on patrol), a hatch per socket-sealing hatch at the SAME socket with the yaw inverse of the doorway's (door yaw + 2), ids unique and resolvable ship-wide with the band contributing no doorway, the facing\u2192yaw table (0/1/2/3), and the engineering high-hatch doorway pinned at its 1.6 m opening / +0.2 m deck offset. COLLISION (2): one box per solid part of every module instance (head 37 + band 28 = 65 on deck 0, storage 102 + 28 = 130 aft) and the band hull spanning its column ([0\u22120.2, 3.2] \u00d7 \u00b10.7). THE GATE (5): the three real ships assemble with assemblyProblems [] (assertAssemblyClean agreeing), the QA rig needs requireValidSpec:false and returns all five STRESS_DEFECTS categories in the assembly view, a bad module ref / a spec naming the shaft / a kit without a shaft module each fail loudly, and the scene graph is JSON-round-trippable with the DeckNode key order the exporter walks."
      ],
      "verification": "npm run lint \u2014 Found 0 warnings and 0 errors (107 files); npx prettier --check src/assembler src/validation/sockets.ts \u2014 clean after --write; npm run typecheck \u2014 tsc -b --noEmit clean; npm run test \u2014 435 passed / 27 files (403 pre-existing + 32 new); npm run build \u2014 check:slots 9/9 then \u2713 built in 340 ms",
      "notes": "New src/assembler/ (8 modules + 32 tests, 2428 lines): assembleShip(spec) is the whole M3-T1 task as one pure headless function. STACK: one DeckNode per spec deck, in spec order, node.floorY/deckId/deckIndex straight off the spec (nothing recomputed downstream). JOIN: refs instantiated at the authored offset+yaw through the M1-T3 resolver (moduleOrigin/moduleDoors \u2014 zero re-derivation), then scanDeckSockets resolves the deck's joins (room spine-door \u2192 band face as kind 'spine'; engaged module\u2194module mates; unjoined sockets come back as legal blanks for M3-T2 to sleeve) with the M0-T2 three-channel mm deviations attached and an `aligned` verdict. SPINE: one band per deck synthesized at the deck-local origin (the spec never references the trunk \u2014 a ref naming it is a reported problem), tiled at the DECK_PITCH_M pitch (spineRunProblems = the vertical half of \u00a78 bullet 3 as assembled geometry sees it). EMIT: per-deck world parts are partitioned into merged GeometryGroups (one per \u00a74 slot, MATERIAL_SLOTS order, deck-scoped ids, ModuleSource provenance) and InstanceBatches of repeated MOULDS (box: yaw excluded \u2192 rides in placement.rotation; cylinder: axis family y vs xz, mould 'x' re-axed by the placement yaw). The pin is exact: placePart(mouldOf(shape), placementFor(shape, part)) \u2261 the world part, so M3-T7 can build one InstancedMesh per batch with no ambiguity. minInstances defaults to 2 and is an option (M3-T7's draw-call lever). INTERACTIVES: kind 'door' per room door socket (socket centre, yaw = facingTurns(facing)) and kind 'hatch' per fillsSocket assembly (same socket, yaw = the hatch's own = doorway yaw + 2, because the leaf faces into the room); the band contributes no doorway (its faces are join interfaces; the room owns the doorway). Equipment slots stay M4 anchors. COLLISION: DeckNode.collision = the modules' own collision hints transformed into world space (one box per solid part, band included) \u2014 M3-T3 owns the match-within-10-cm invariant over this hull, so the assembler only guarantees it is the hints, placed and finite. MEASURED (probe, then deleted): Patrol 5 decks / 706 parts \u2192 29 groups + 140 batches = 169 draw calls (\u2264 the 250 M3 ceiling, before M3-T7's own merge pass), 5 spine joins, 19 blanks, 18 interactives (9 doors + 9 hatches), 423 hull boxes; Long-Haul 896 parts / 201 calls; Science 646 parts / 171 calls; rig-3 (two rooms on one deck) 227 parts / 49 calls / 2 joins. NOTES: assembleShip validates first by default (assertValidShipSpec against the resolved kit) \u2014 requireValidSpec:false is the QA-rig opt-in; a custom `modules` list derives its own manifest so the two cannot disagree. Also touched src/validation/sockets.ts: toMm now normalizes \u22120 \u2192 0 (a deviation rounding to zero from below was reported as signed zero, which Object.is-strict matchers and M3-T2's deviation rows both misread) \u2014 the repo's standard converter fix, no value change otherwise. Not wired into the app: src/assembler is pure data (like src/kit before M2-T2) \u2014 nothing imports it yet, so vite does not bundle it and Viewport stays the empty shell until M3-T7/M4 render the graph. NEXT: M3-T2"
    },
    "M3-T2": {
      "status": "complete",
      "date": "2026-09-19",
      "title": "Seam/hatch enforcement: mating geometry generated from sockets (never freehand); [auto] watertight + alignment checks wired into the assembler",
      "files_modified": [
        "src/assembler/seams.ts",
        "src/assembler/seams.test.ts",
        "src/assembler/types.ts",
        "src/assembler/assemble.ts",
        "src/assembler/checks.ts",
        "src/assembler/index.ts",
        "src/assembler/assembler.test.ts",
        "src/invariants/registry.ts",
        "src/invariants/checks.ts",
        "src/invariants/index.ts",
        "src/invariants/invariants.test.ts",
        "README.md"
      ],
      "tests_added": [
        "src/assembler/seams.test.ts \u2014 26 tests in six groups. PLANES (3): every facing mapped to its wall-plane axis / normal axis / sign; a placed module's face rectangle + plane read off its world box (the room's 4.2 x 3.0 x 5.0 block and the 1.4 x 3.2 x 1.4 shaft band column at the deck-local origin, a door's face rect = its facing's face rect); the opening rect cut from the socket's own size/centre (standard 0.9 x 2.0 door standing on the floor line; the engineering high-hatch 0.8 x 1.6 at the 1.2 m centre on the +x face). RECTANGLES + ANNULUS (3): intersect/union/contains/overlap-area/grow behaviour incl. disjoint + cross-plane rejection; the annulus decomposition (standard doorway \u2192 jambs + lintel and NO sill on the floor line; raised doorway \u2192 sill; opening == contact \u2192 nothing; opening missing the contact \u2192 the whole contact as wall); overshoot touches ONLY free edges. SLEEVES (4): the spine join's plan (gap == the join's own normalMm == 0, 3 required bands == 3 parts, both bites 30 mm, room owns the doorway as the parts' source, all parts bulkhead-slot boxes with a mould key, the 60 mm plate centred inside the joint plane, jamb 0.25+overhang x 3.0+2*overhang, lintel exactly the 0.9 m doorway, zero overlap between any sleeve part and the pass-through); every join of every canonical fixture has a plan whose gap equals |join.normalMm| and a plan exists per join + per blank socket (blanks + 2*joins == sockets); determinism across two assembles + world-space measurement on the band's own faces (plane 0.7, door centre = floor+1.0 on decks 1..4); the QA rig's module-to-module mate (gap 0, union opening 0.9 x 2.0 from the mismatched high-hatch/side-door pair, no part inside it). BLANKED SOCKETS (5): the shaft's 3 unjoined faces per deck are plugged \u2014 one solid part per plan, cut from the socket (opening lapped by the 20 mm lip on every side), filling the wall it sits in (the kit's 0.1 m wall MEASURED off the band's geometry) and bitten past both faces, with all three plugs sharing one mould \u2192 one instance batch of 3; wallExtentOf measures the wall a socket sits in (bulkhead panel 0.6..0.7 for the band, 0.1 m thick and flush with the face for a room's side door) rather than assuming a thickness; a socket the module already hatches is left to the module (sealedBy 'hatch', 0 generated parts, the module's own placed hatch parts as evidence, straddling the socket plane); a join with no owner module comes back as a reported 'unresolved' plan (no invented geometry); a stripped owner \u2192 wallExtentOf undefined \u2192 no owner lookup. MEASUREMENT (4): the coverage/bite/passage measurement is real logic \u2014 a covering seal with both bites 30 mm passes; a plate starting 2.5 mm inside the joint plane is caught as an open seam with a negative bite; an uncovered 0.250 x 3.000 m band, missing geometry ('no sealing geometry was generated') and a seal that grows into the doorway (mm\u00b2 intruded) are each caught; a plug is measured against the wall it fills (a shallow plate fails 'does not fill the wall it sits in', an offset plate fails the opening coverage). BULKHEAD MEETS (2): a yawed second galley whose wall lands on the first one's +x face with no socket between passes flush and is reported as a 5.0 mm gap over the watertight cap when pushed off; a pair that already owns a join is never re-reported (all three real ships clean). THE GATE (4): the three real ships come back watertight with every socket closed (tally: joins == deck joins, blanks == deck blanks, sleeved == joins, hatchSealed + plugged == blanks, max gap 0.0 mm, min bite 30 mm, part count == the plans' own); Patrol = 5 joins / 19 blanks (4 own-hatch + 15 plugged) / 30 generated seam parts; the QA rig fails on exactly one open seam (rig-1 10.0 mm) while rig-2/rig-3 (25 mm lateral, 200 mm door-center step) and rig-4 (200 mm vertical drift) stay geometric non-issues for THIS invariant \u2014 their defects are alignment/run findings; every generated part is a bulkhead-slot box from the \u00a74 vocabulary and every 'hatch' plan's evidence is literally the module's own placed parts (identity, not a re-generation); each sleeve member reproduces its required band (with the overshoot).",
        "src/assembler/assembler.test.ts \u2014 updated 3 existing tests for the partitioned seam parts (the geometry partition is now modules + generated seams: placedPartsOf, module-part counts still pinned to the kit) and added 2 seam-pass integration tests (6 total new claims): every real ship gives one seam plan per socket (plans == blanks + joins == sockets, each plan watertight with no problems and a seal, every generated part inside the deck's partition) and the assembler gate stays clean; the QA rig's plan for rig-1 reports 10 mm and the gate names the open seam against the 2 mm cap.",
        "src/invariants/invariants.test.ts \u2014 seams-watertight is now LIVE: the live/stub split is five live + collision-match (the stub-target rule now only applies to it), the real-ship harness row is [pass, pass, pass, deferred, pass, pass] and the stress row [fail, fail, fail, deferred, fail, pass], the deferred-owners map drops seams-watertight, and a new describe adds 4 tests: the pass detail's measured tally on all three real ships (max gap 0.0 mm < 2 mm cap, min bite 30.0 mm, 5 joins / 19 blanked sockets / 4 own-hatch / 15 plugged on Patrol); the stress rig fails naming rig-1's 10.0 mm open seam between the shaft band's +z face and head#0's spine-door while rig-2/rig-4 stay out of this invariant's findings; a synthetic ship with the crew galley pushed 10 mm proud of the spine fails where its canonical twin passes (not fixture-shaped); a spec referencing a kit-unknown module fails with 'cannot be assembled' instead of throwing."
      ],
      "verification": "npm run lint \u2014 Found 0 warnings and 0 errors (109 files, 104 rules); npx prettier --check src/assembler src/invariants \u2014 clean after --write on the 4 touched files; npm run typecheck \u2014 tsc -b --noEmit clean; npm run test \u2014 467 passed / 28 files (435 pre-existing + 32 new/updated); npm run build \u2014 check:slots 9/9 assigned then \u2713 built in 336 ms (only the pre-existing >500 kB chunk warning).",
      "notes": "DONE 2026-09-19: M3-T2 \u2014 SEAM/HATCH ENFORCEMENT, headless, generated from sockets. New src/assembler/seams.ts (the M0-T2 decision-1 rule as code): every join's mating geometry is DERIVED from its two DoorSockets \u2014 no freehand geometry anywhere. A SLEEVE is the contact annulus (the intersection of the two owner modules' declared face rectangles minus the doorway) decomposed exactly the way the M2-T1 bulkheadParts builder cuts a wall (jambs spanning the full contact height, a sill/lintel only where the doorway does not reach the panel edge \u2014 a standard 0.9 x 2.0 door on the floor line therefore yields 3 members), extruded along the join normal from planeA-30mm to planeB+30mm (SEAM_BITE_M, 6x the 5 mm placement cap) and authored IN THE SOCKET'S OWN FRAME (local +Z = the wall normal) exactly like the kit's hatch, so the same plate across two facings is one mould and the M3-T1 partition batches them (Patrol: 30 seam parts cost only 10 extra draw calls: 736 parts / 179 calls vs the M3 ceiling of 250). Free edges overshoot the contact region by 10 mm (SEAM_OVERHANG_M) so no sleeve face lands coplanar on a module face, while inner edges sit exactly on the doorway edge so the passage is untouched; the sleeve lives inside the two walls' own thickness (it IS the joint). A BLANKED socket is closed by the owner module's own fillsSocket hatch when it authors one (sealedBy 'hatch' \u2014 recorded as EVIDENCE (the module's own placed parts, identity-compared) and never re-emitted), otherwise by a PLUG: a blanking sleeve cut from the socket's opening, lapped 20 mm (SEAM_PLUG_LIP_M) onto the surrounding wall and extruded to fill the wall's MEASURED thickness plus a 30 mm bite on both faces (wallExtentOf reads the wall off the module's assembled geometry \u2014 the thinnest part straddling the socket plane inward whose in-plane footprint shares an edge with the opening, i.e. the panel the doorway is cut in, not the opening; the kit's 0.1 m bulkhead measures exactly). Plugs are solid:true \u2014 M3-T3 folds seamSolidParts() into the deck hull. MEASUREMENT (measureSealCoverage, exported): per required band \u2014 covered by an emitted part? does the covering part reach past BOTH wall planes (the bite)? is the plug's wall interval spanned? does any part intrude into the pass-through (\u00a78: the doorway must stay exactly as wide as the sockets say)? Plus bulkheadMeetProblems: \u00a78 bullet 1's 'bulkhead meet' half \u2014 module faces that engage within SOCKET_ENGAGE_RADIUS_MM with overlapping rectangles but NO door-socket join must be gap-free (joined pairs are skipped: a join already owns its seam). \u00a78 BULLET 1 NOW LIVE (owner M3-T2): src/invariants/checks.ts checkSeamsWatertight assembles the spec itself (requireValidSpec:false so a rejected rig still reports its geometry, and a spec that cannot assemble fails with a reason instead of throwing) and reads the seam verdicts; registry entry flipped stub\u2192live with only collision-match (M3-T3) left as a declared target \u2014 the harness rows are now [pass,pass,pass,deferred,pass,pass] on the real ships and [fail,fail,fail,deferred,fail,pass] on the stress rig. THE ASSEMBLER GATE also carries both halves now (checks.ts step 9): one sleeve plan per join with its measured gap cross-checked against the join's own along-normal channel, one plan per blanked socket, every seal's own problems surfaced verbatim, and the alignment half stays the join rules (section 8). FIXTURE VERDICTS: Firebrand 5 joins / 19 blanks (4 own-hatch, 15 plugged) / 30 seam parts / max gap 0.0 mm / min bite 30.0 mm; Vagabond 6/23/36; Surveyor 5/19/30 \u2014 all clean. The QA rig fails exactly one seam: rig-1's 10 mm proud head door (an open 10 mm slot between the mating wall faces, > the 2 mm cap). rig-2/rig-3/rig-4 are geometrically SEALED (their wall planes coincide; a lateral shift or a door-center step blocks part of a doorway but opens no slot) \u2014 their defects remain the validator's and the assembler's alignment/run findings, and the pass detail says so. Also refactored: the hatch\u2194socket pairing rule (the module owns the hatch on its own socket, M2-T2) now lives once in seams.ts (selfSealingAssemblyOf) and both interactivesOf and the seam pass use it (no forked pairing logic); placedPartsOf() is the single part list the partition and the gate count. NEXT: M3-T3 \u2014 collision hulls per deck (from module collision hints + the seam plugs' solid parts), owner of the last stub invariant (collision-match, \u2264 10 cm per deck)."
    },
    "M3-T3": {
      "status": "complete",
      "date": "2026-09-20",
      "title": "Collision hulls per deck (from module collision hints) + the \u00a78 bullet-4 collision-match invariant live",
      "files_modified": [
        "src/assembler/collision.ts",
        "src/assembler/collision.test.ts",
        "src/assembler/place.ts",
        "src/assembler/assemble.ts",
        "src/assembler/checks.ts",
        "src/assembler/seams.ts",
        "src/assembler/index.ts",
        "src/assembler/assembler.test.ts",
        "src/invariants/checks.ts",
        "src/invariants/registry.ts",
        "src/invariants/index.ts",
        "src/invariants/invariants.test.ts"
      ],
      "tests_added": "src/assembler/collision.test.ts (NEW) \u2014 16 tests in four groups. COMPOSITION (4): the deck hull is the modules' own hints placed in module order (rooms then the band) then the generated solid seam parts, and it is exactly what the node's collision.boxes carries (\u2264 Patrol: 68/85/70/82/133 boxes per deck = 65/82/67/79/130 hint boxes + 3 plugs each, 438 total = 423 module + 15 plug, over 5 joins; Vagabond 571, Surveyor 375); every hint box is paired with the placed solid part it stands for with 0.000 mm deviation on every deck of every fixture; each plug covers its doorway's whole opening and spans past both wall faces; seamSolidParts() is the same predicate the hull uses. THE MEASUREMENT (5): hullDeviation is two-way (geometry poking out = uncovered, box reaching past = excess) on every axis, overlapDepth is a true volumetric overlap (touching = 0, separated < 0), all four fixtures report 0.0 mm and no problems, a hint box grown 200 mm is caught with its measured uncovered/excess split against the \u2264 100 mm cap (canonical kit on the same spec stays clean), a hint that over-declares a box nothing draws is caught as a phantom wall, and the 10 cm constant is pinned. PASS-THROUGH (3): one volume per join on every fixture with all of them clear; Patrol deck 0's spine pass-through measured exactly (0.9 \u00d7 2.0 opening on the floor line, z 0.7 \u00b1 the 30 mm seal reach); a hull box in the doorway (the socket hatch's own bounds injected into the head's hint) is caught at 60.0 mm with 'the walker cannot pass', while the canonical head passes. VERDICT SURFACE (2): assemblyProblems() surfaces the same bullet-4 findings on the doctored kit and is empty on the three real ships; the QA rig hulls clean on every deck (its declared defects stay alignment/run/seam findings). src/invariants/invariants.test.ts \u2014 47 tests: the stub pin became 'all six live, no stub remains', the harness rows became all-pass for the real ships and [fail, fail, fail, pass, fail, pass] for the rig, the deferred-run test was replaced by 'no run is deferred', the per-invariant column test gained collision-match, and a new four-test group covers checkCollisionMatch (tally text, harness verdict, a doorway-box kit failure, and the cannot-be-assembled path).",
      "verification": "npm run lint \u2014 Found 0 warnings and 0 errors (111 files, 104 rules); npx prettier --check on the touched files, --write on the 4 it flagged, then npm run format:check \u2014 repo-wide clean; npm run typecheck \u2014 tsc -b --noEmit clean; npm run test \u2014 487 passed / 29 files (467 pre-existing + 20 new: 16 in src/assembler/collision.test.ts, 4 net in src/invariants/invariants.test.ts); npm run build \u2014 check:slots 9/9 assigned, tsc, then \u2713 built in 348 ms (only the pre-existing >500 kB chunk warning).",
      "notes": "DONE 2026-09-20: M3-T3 \u2014 COLLISION HULLS PER DECK, and the LAST \u00a78 [auto] invariant went live: collision-match is no longer a stub, so the harness reports six verdicts and no `deferred` run exists any more. New src/assembler/collision.ts is the whole task as one headless module set. COMPOSITION: deckHull(modules, seams) is the deck's hull box by box, with the visible geometry each box stands for: (1) every module instance's OWN collision hint (M1-T2 KitModule.collisionHint, authored in M2 as one box per `solid` part \u2014 a coarse per-assembly box would seal the doorway the assembly is cut around) placed through transformAabb(origin, yaw), in module order rooms-then-band; (2) the GENERATED seam geometry that must block a walker \u2014 M3-T2's blanking plugs (SeamPlan.solid, the same predicate `seamSolidParts` reads; the sleeves of joins stay solid:false because they are the wall annulus the wall parts' own boxes already cover, and hatch leaves stay out because their collision is the M3-T5 state machine's). deckHullBoxes() is the plain list DeckNode.collision.boxes now carries (assemble.ts), so the M1-T2 scene-graph contract gained the plug boxes: Patrol deck 0 37+28 hint boxes + 3 plugs = 68, deck 4 102+28+3 = 133, Patrol 438 boxes total / 15 plugs. MEASUREMENT (\u00a78 bullet 4, 'collision hull matches visible geometry within 10 cm per deck'): hullDeviation(box, geometry) is two-way \u2014 uncoveredM (the visible part pokes OUT of the box: a walker could clip through the wall) and excessM (the box reaches PAST the part: an invisible wall) \u2014 max() of the two is the number the cap is measured on; sub-nanometre differences between the two independent bounds paths (transformAabb on the hint vs partBounds on the placed part) are snapped to 0 (DEVIATION_EPS_M, three orders under the 0.001 mm report resolution). PASSAGE: a join's pass-through volume is the opening the two sockets share extruded along the join normal from one 30 mm SEAM_BITE_M before the near wall plane to one past the far one (PASSAGE_REACH_M \u2014 a zero-thickness plane would make the check vacuous), and no hull box may ENTER it beyond the kit's \u00b10.5 mm socket-authoring budget (touching is legal: a pier's face lies exactly on the opening edge). collisionProblems(ship)/deckCollisionProblems(deck) report completeness (one box per solid part + plug), the match, the passage, and phantom boxes with no geometry; collisionTally(ship)/deckCollisionTally(deck) are the report surface (per-deck rows + maxima) and assemblyProblems() gained rule 11 (so the assembler gate and the invariant check read exactly the same verdict). MEASURED on the four canonical fixtures: every deck complete, max deviation 0.0 mm, every pass-through clear \u2014 including the QA rig, whose declared defects are alignment/run/seam findings (rig-1 10 mm open seam, rig-2 25 mm lateral, rig-3 200 mm door step, rig-4 200 mm floor pin + 50 mm off-grid) and whose kit and hull boxes move together, so bullet 4 passes there too. That is the honest verdict: no spec-only change can mismatch a deck's hull (the hull is derived from the same geometry it describes), so the check's failure modes are kit regressions and doorway-blocking boxes \u2014 both pinned by failure-injection tests in collision.test.ts (a hint box grown 200 mm, a hint that leaves a solid part unhulled, and the socket hatch's own bounds injected as a hull box, which is caught standing 60.0 mm inside the spine pass-through). Refactor: place.ts gained placedPartOf() + worldSolidPartsOf() so the hull's geometry side is built by the same transform path as every other placed part, and seams.ts's seamPartsOf/seamSolidParts widened to Pick<DeckAssembly, 'seams'>. Also migrated: invariants/registry.ts collision-match is status 'live' with its limit imported from COLLISION_MATCH_TOLERANCE_M (no drift), the registry/checks/index header docs now say six live checks and no stubs, and the pins in invariants.test.ts were updated (stubInvariants() === [], real-ship row all-pass, rig row [fail, fail, fail, pass, fail, pass], 'no run is deferred'). NEXT: M3-T4 \u2014 first-person rig + under-burn gravity (depends M3-T1, done): the movement/gravity layer M3-T5's ladder state machine and M3-T6's spawn selection both need. M3-T7 (draw-call pass, also depends only on M3-T1) stays available in parallel; the M0 held visual spikes (T1 wayfinding, T3 galley lighting, T4 ladder feel) are still unchecked for a human interactive session \u2014 they block nothing."
    }
  }
}
